# 결정 이력

설계 결정과 **그 이유**를 남긴다. 무엇을 했는지가 아니라 **왜 그렇게 했는지**, 그리고 **무엇을 버렸는지**.
스키마 구조나 코드 설명은 여기 적지 않는다 — DDL과 스크립트가 진실이다.

형식: `## YYYY-MM-DD · 제목` → 결정 / 이유 / 대안 / 영향

---

## 2026-07-29 · 하트와 포인트를 하나로 통합

**결정** — 두 개의 통화를 두지 않고 `user.heart_balance` 하나로 관리한다.

**이유** — 기존 DB에서 이미 사실상 같은 것이었다. `accounts_user.point` 컬럼과 핵클의
`heart_balance`가 같은 유저에서 값이 일치했다(user 833041: 885,000,006 vs 884,999,804).
UI는 "하트"라 부르고 DB 컬럼명은 `point`여서 분석할 때마다 혼선이 있었다.

**대안** — 포인트(적립 전용)와 하트(구매 전용)를 분리. 기각한 이유는 실제 사용처가
구분되지 않아 분리해도 두 잔액이 같이 움직일 뿐이었기 때문.

**영향** — `heart_transaction` 하나가 모든 증감을 기록한다. 적립·차감·충전·광고보상이 전부 여기 모인다.

---

## 2026-07-29 · 모든 하트 증감에 `balance_after`를 기록

**결정** — 거래 행마다 거래 직후 잔액을 함께 저장한다.

**이유** — 기존 원장은 순합계가 201만인데 유저 잔액 총합은 20억이었다. 가입 지급과 하트 충전이
원장에 남지 않아서다(heart.777이 57,873건 팔렸는데 원장의 +777 행은 21건뿐). 잔액을 원장으로
재구성하는 게 불가능했다.

**대안** — 원장만 두고 잔액은 매번 합산해서 계산. 기각. 데이터가 커지면 느리고,
무엇보다 **누락을 탐지할 방법이 없다**.

**영향** — `heart_transaction.balance_after`와 `user.heart_balance`를 대조하면 정합성이 즉시 검증된다.
모든 증감이 예외 없이 이 테이블을 거쳐야 한다는 규칙이 생긴다.

---

## 2026-07-29 · 탈퇴 기록에 유저 식별자를 넣는다

**결정** — `user_withdrawal.user_id`를 NOT NULL FK로 두고, 자유 서술 `reason_text`도 받는다.

**이유** — 기존 `accounts_userwithdraw`는 70,764건(가입자의 10.5%)인데 `reason`과 `created_at`뿐이라
누가 탈퇴했는지 특정할 수 없었다. 탈퇴 사유를 유저 속성과 교차분석하는 게 원천 봉쇄됐다.
게다가 사유의 57%가 "기타 이유"라 실질 정보가 거의 없었다.

**대안** — 개인정보 보호를 위해 익명 유지. 기각. 탈퇴자 데이터는 보관 기간을 짧게 두고
파기하는 방식으로 관리하면 되고, 분석 불가능한 데이터를 쌓는 것보다 낫다.

**영향** — 탈퇴 사유별 코호트 분석이 가능해진다. `reason_text`로 "기타" 블랙박스를 연다.

---

## 2026-07-29 · 셔플은 DB 제약으로 1회를 강제한다

**결정** — `vote_shuffle.vote_item_id`에 UNIQUE, `ad_impression_id`는 NOT NULL FK.

**이유** — "셔플 1회, 광고 시청 필수"를 애플리케이션 코드로만 막으면 우회 경로가 생긴다.
스키마로 막으면 코드에 버그가 있어도 데이터가 오염되지 않는다.

**영향** — 광고를 보지 않고 셔플한 기록은 구조적으로 존재할 수 없다.

---

## 2026-07-29 · 후보 4명을 행으로 저장하고 셔플 전후를 모두 남긴다

**결정** — `vote_candidate`에 `shuffle_round` 컬럼을 두어 최초 후보(0)와 셔플 후 후보(1)를 모두 보존.

**이유** — 기존엔 세트↔피스 관계가 `longtext` JSON 배열이라 SQL로 펼칠 수 없었다.
또 "어떤 후보였을 때 셔플을 눌렀는가"는 광고 수익과 직결되는 분석인데 데이터가 없으면 못 본다.

**영향** — 셔플 유발 요인 분석이 가능해진다. 대신 후보 행 수가 최대 2배가 된다.

---

## 2026-07-29 · 신고와 제재를 FK로 연결하고 정책을 데이터로 정의

**결정** — `sanction.triggered_by_report_id`로 근거 신고를 명시하고, `sanction_policy` 테이블에
자동 제재 임계값을 저장한다.

**이유** — 기존 시스템은 신고와 제재가 완전히 단절돼 있었다. 피신고 10회 이상 116명 중 제재된 사람이
0명이고, 253회 신고받은 유저도 정상 상태로 활동 중이었다. 임계값이 코드에만 있으면(혹은 없으면)
운영 정책을 감사할 수 없다.

**영향** — "몇 회 신고 시 어떤 조치"가 스키마에 드러난다. 다만 **정책이 있어도 검토할 사람이 없으면
의미 없다** — 그래서 익명 게시판을 v1에서 뺐다.

---

## 2026-07-29 · 익명 게시판을 v1에서 제외

**결정** — 스키마에는 설계해두되 v1 서비스에서는 열지 않는다.

**이유** — 익명 게시판은 모니터링 인력이 필수다. 기존 서비스에서 신고 시스템이 사실상 작동하지 않았던
전례가 있고, 1인 프로젝트로는 신고 검토를 감당할 수 없다. 사고가 났을 때 책임을 질 수 없는 기능은 열지 않는다.

**대안** — 열되 신고를 자동 숨김 처리. 기각. 자동 숨김은 악용(집단 신고)에 취약하다.

**재검토 조건** (2026-07-29 추가) — 아래 둘이 **동시에** 충족될 때만 다시 본다.
신고 검토를 실제로 맡을 사람이 있을 것, 그리고 이용자가 성인으로 유지될 것.
하나라도 빠지면 열지 않는다. 스키마(`post`, `post_comment`, `post_like`, `comment_like`)는
지우지 않고 그대로 둔다 — 판단이 바뀌는 것은 인력 문제이지 설계 문제가 아니기 때문이다.

**방향** (2026-07-29 작업자 확인) — **언젠가는 연다.** 다만 익명 그대로가 아니라
**자유게시판**(글쓴이의 닉네임이 드러나는) 형태로 바꿔서라도 연다.
글에 작성자가 붙으면 모니터링 부담이 크게 준다 — 익명 게시판을 막았던 이유가
"사고가 나도 책임을 물을 수 없다"였는데, 그 전제가 바뀌기 때문이다.

스키마는 이미 그렇게 되어 있다. `post.author_id` 는 NOT NULL 이고
(60_board.sql 주석: *"화면에는 익명으로 표시하되 author_id 는 내부에 남긴다"*),
익명 표시 여부는 **화면의 선택**이다. 자유게시판으로 열 때 DDL 변경은 필요 없다.

---

## 2026-07-29 · 성인 지인 대상 클로즈드 테스트로 한정

**결정** — 링크를 아는 성인 지인 20~50명만 사용. 불특정 다수 공개 안 함.

**이유** — 원 서비스는 중·고등학생 대상이었다. 미성년자 데이터는 만 14세 미만 법정대리인 동의가
필요하고, 외모 관련 투표와 익명 게시판을 미성년자에게 열면서 모니터링이 없으면 감당할 수 없다.
성인 지인으로 한정하면 동의 취득이 실질적으로 가능하고 사고 위험이 크게 준다.

**영향** — 실유저 데이터 규모가 작다(수천 행). 그래서 파이프라인 학습용 합성 데이터가 별도로 필요해졌다.

---

## 2026-07-29 · 합성 데이터와 실유저 데이터를 분리

**결정** — 운영 DB(Supabase)에는 실유저만. 합성 데이터는 별도 스키마/프로젝트에 두고,
BigQuery 적재 시 `is_synthetic` 플래그로 구분한다. 합성 규모는 유저 5,000명 / 3개월치.

**이유** — 섞으면 실유저가 존재하지 않는 가짜 유저를 투표 후보로 보게 된다. 분석도 오염된다.
반대로 합성 데이터가 아예 없으면 지인 30명이 만드는 수천 행으로는 Airflow가 할 일이 없어
증분 적재·스케줄링·재시도를 연습할 수 없다.

**대안** — 유저 5만 명 / 6개월치. 규모를 줄인 이유는 생성 시간과 무료 티어 여유 때문이고,
5,000명 / 3개월이면 파이프라인 검증에 충분하다.

---

## 2026-07-29 · Supabase 연결은 Session pooler 를 쓴다

**결정** — 직접 연결(`db.<ref>.supabase.co`) 대신 Session pooler
(`aws-1-ap-northeast-2.pooler.supabase.com:5432`)로 접속한다.

**이유** — 무료 플랜의 직접 연결 엔드포인트는 **IPv6 전용**이다. DNS 조회 결과가
AAAA 레코드뿐이라 IPv4 환경에서는 호스트 이름 해석 자체가 실패한다.
Supabase 는 IPv4 전용 애드온($4/월)을 팔지만 **Pro 플랜($25/월) 가입이 전제**라
"무료 티어를 벗어나지 않는다"는 제약과 충돌한다.
공유 pooler 는 IPv4 를 기본 지원하며 추가 비용이 없다.

**대안** — Transaction pooler(6543). 기각. 상태를 유지하지 않는 연결이라
대량 적재나 준비된 구문에서 문제가 생긴다.

**함께 겪은 것** — DB 비밀번호에 `@` 가 들어 있어 접속 URI 가 잘못 파싱됐다.
URI 안의 비밀번호는 퍼센트 인코딩이 필요하다(`@` → `%40`).
`db/apply.py` 등은 `.env` 값을 그대로 쓰므로 인코딩된 상태로 보관한다.

---

## 2026-07-29 · 클라이언트에 쓰기 권한을 거의 주지 않는다

**결정** — RLS 는 읽기 위주로 열고, 하트·투표처럼 규칙이 있는 조작은
클라이언트가 직접 INSERT/UPDATE 하지 못하게 막는다. 나중에 RPC 함수로 처리한다.

**이유** — Supabase 는 브라우저가 DB에 직접 말을 건다. `heart_transaction` INSERT 를
열어주면 누구나 하트를 무한정 만들 수 있고, `app_user.heart_balance` UPDATE 를
열어주면 잔액을 마음대로 바꿀 수 있다. 실제로 침투 시험에서 두 시도 모두
`InsufficientPrivilege` 로 막히는 것을 확인했다.

**구현** — `REVOKE UPDATE ON app_user FROM authenticated` 후
`GRANT UPDATE (nickname, class_id, gender)` 로 **컬럼 단위**로만 허용했다.
RLS 는 행 단위라 컬럼을 제한할 수 없어 GRANT 와 함께 써야 한다.

---

## 2026-07-29 · 투표자 신원은 뷰로만 노출한다

**결정** — `vote_received` 테이블은 클라이언트가 직접 조회할 수 없다(정책 없음 = 차단).
대신 `my_vote_received` 뷰로만 접근하며, 이 뷰가 `reveal_status` 에 따라
`voter_id` 를 가린다.

**이유** — "누가 나를 뽑았는가"가 하트를 받고 파는 유료 정보다.
지목당한 사람이 테이블을 직접 읽을 수 있으면 서비스의 수익 구조가 무너진다.
**RLS 는 행 단위라 컬럼을 숨길 수 없다.** 그래서 테이블 접근을 막고
필요한 컬럼만 계산해 내보내는 뷰를 둔다.

- `HIDDEN` → 아무 정보 없음
- `PARTIAL` → 닉네임 첫 글자만 (`voter_initial`)
- `REVEALED` → `voter_id` 공개

침투 시험에서 미공개 상태의 `voter_id` 조회가 막히고,
부분공개에서는 초성만 나오는 것을 확인했다.

---

## 2026-07-29 · 웹앱을 최우선 트랙으로 재편

**결정** — 파이프라인(P3~P7)을 뒤로 미루고 웹앱 MVP를 먼저 만든다.

**이유** — v1 설계서에는 **접속해서 볼 수 있는 화면이 진행 순서에 아예 없었다.**
9단계를 다 끝내도 나오는 것은 BigQuery 테이블과 대시보드뿐이었다. 대화에서는
3단계(파이프라인 → 웹앱 → 배포)로 제안해놓고 문서에는 파이프라인만 담은 누락이다.

두 달간 눈에 보이는 결과가 없으면 프로젝트가 추상적으로만 느껴져 흐지부지되기 쉽다.
또 **웹앱을 만들어봐야 스키마의 진짜 문제가 드러난다** — 실제로 "친구를 어떻게 추가하지?"라는
질문에서 초대 코드 개념이 스키마에 없다는 게 바로 드러났다.

**영향** — P0~P2가 이미 끝나 786만 행이 DB에 있으므로 여기서 멈춰도 손실이 없다.
파이프라인은 웹앱 배포 후 재개하며, 그때는 실유저 데이터가 함께 흐른다.

---

## 2026-07-29 · 개인정보를 일절 받지 않는 익명 인증

**결정** — 이메일·전화번호·실명·비밀번호를 받지 않는다.
Supabase Anonymous Sign-in으로 접속 즉시 계정이 생기고, 유저가 입력하는 것은
**닉네임과 소속(학교·반)뿐**이다. 친구는 **초대 코드**를 주고받아 맺는다.

**이유** — MVP 단계에서 개인정보를 받으면 수집·보관·파기 의무가 생기고,
유출 시 책임이 따른다. 받지 않으면 그 위험 자체가 사라진다.
로그인 절차가 없어 진입 장벽도 낮아진다.

**대안** — 이메일 매직링크. 기각. 이메일은 개인정보이고, 메일함을 오가는 절차가
지인 대상 가벼운 테스트에는 과하다.

**영향**
- `app_user.phone_hash` 제거, `name_masked` → `nickname`, `invite_code` 추가
- `user_contact`(전화번호 해시)는 MVP DDL에서 생성하지 않는다
- `friend_recommendation.reason = MUTUAL_CONTACT`는 MVP에서 사용하지 않는다
- **알려진 한계**: 익명 계정은 브라우저에 귀속되어 캐시를 지우면 사라진다.
  MVP에서는 감수하고 안내 문구로 알린다. 복구 코드는 v2.
- ⚠️ "닉네임 + 학교 + 반"은 소규모 집단에서 개인 특정이 가능하므로,
  법적으로 비개인정보라고 단정하지 않고 개인정보처리방침에 수집 항목을 명시한다.

> **갱신 (2026-07-29)** — 입력 항목에 **성별**이 추가됐다. 아래 "성별을 온보딩에서
> 받는다" 참조. 이메일·전화번호·실명을 받지 않는다는 결정 자체는 그대로다.

---

## 2026-07-29 · MVP에서 광고와 결제를 스텁으로 처리

**결정** — 셔플에 필요한 광고는 3초 대기 스텁으로, 하트 충전은 아예 제외한다.

**이유** — AdMob·인앱결제는 계정 개설과 심사가 필요해 MVP 일정에 맞지 않는다.
스키마(`ad_impression`, `heart_purchase`)는 이미 준비돼 있으므로 나중에
실제 SDK를 붙일 때 구조를 바꿀 필요가 없다.

**영향** — MVP에서 하트는 가입 지급과 투표 적립으로만 늘어난다.
힌트를 많이 사면 잔액이 마르는데, 이는 오히려 하트 경제의 균형을 관찰할 기회다.

---

## 2026-07-29 · `GLOBAL` 스코프는 "내 친구 전체"

**결정** — 세 스코프의 후보 풀을 이렇게 정의한다.
`CLASS` = 같은 반 친구 / `SCHOOL` = 같은 학교 친구 / `GLOBAL` = 학교 무관 친구 전체.
**세 스코프 모두 친구 관계가 전제**이며, 모르는 사람은 어떤 경우에도 후보로 뜨지 않는다.

**이유** — 기존 서비스 데이터에서 후보의 98.5%가 실제로 투표자의 친구였다(친구 풀에서 뽑는 구조였음).
그리고 차단 사유 1위가 "모르는 사람임"(49.5%)이었다. 전체 가입자를 후보로 넣으면
모르는 사람이 뜨고 차단·신고가 늘어난다.

**대안** — `GLOBAL`을 전체 가입자로 정의. 기각. 위 이유에 더해, 모르는 사람에 대한 투표는
서비스 취지(아는 사이의 가벼운 인정)와 맞지 않는다.

**영향** — 친구 5명 게이트가 후보 풀의 하한을 보장한다.
합성 데이터 생성 시 친구 그래프를 먼저 만들고 그 위에서 후보를 뽑아야 한다(생성 순서 제약).

---

## 2026-07-29 · 순환 FK를 제거하는 방향으로 스키마 정리

**결정** — DDL 작성 과정에서 양방향으로 참조하던 3쌍을 단방향으로 정리했다.

- `question.origin_request_id` 제거 → `question_request.published_question_id`만 유지
- `vote_item.chosen_candidate_id` 제거 → `vote_candidate.is_chosen` 플래그 + 부분 유니크 인덱스
- `hint_purchase.heart_transaction_id` 제거 → `heart_transaction.hint_purchase_id`만 유지

**이유** — 순환 FK는 테이블 생성 순서를 꼬이게 하고, 두 방향이 어긋났을 때 어느 쪽이 진실인지
알 수 없다. 원장(`heart_transaction`)이 원인을 가리키는 단방향이 자연스럽다.

**영향** — "선택된 후보"는 `vote_candidate.is_chosen = true`로 조회한다.
아이템당 1명만 선택되는 것은 부분 유니크 인덱스가 강제한다.

---

## 2026-07-29 · Cloud Composer 대신 로컬 Docker Airflow

**결정** — 관리형 Airflow를 쓰지 않는다.

**이유** — Cloud Composer는 최소 구성도 월 40만원대다. 학습·포트폴리오 목적에 비해 비용이 과하다.
로컬 Docker면 0원이고, 나중에 필요하면 작은 VM(월 3~4만원)으로 옮기면 된다.

**영향** — 스케줄이 PC가 켜져 있을 때만 돈다. 실서비스 운영에는 부적합하지만 현재 범위에서는 문제없다.

---

## 2026-07-29 · 성별을 온보딩에서 받는다

**결정** — `app_user` 의 유저 입력 컬럼(닉네임·성별·소속)을 모두 받는다.
성별은 온보딩 필수 항목이며, 서버(`complete_onboarding`)가 비어 있으면 거부한다.

**이유** — 성별은 받은 투표의 **힌트로 파는 정보**다. 구 서비스 실측 누진 요금
200→300→500→1000 의 두 번째 단계가 GENDER 였는데, 성별을 받지 않는 동안에는
팔 것이 없어 3단계로 줄여두었다. 없는 정보를 하트를 받고 팔 수는 없기 때문이다.
성별을 받으면 이 단계가 되살아나고, 수익 구조를 원 서비스와 같은 모양으로 시험할 수 있다.

**영향**
- 힌트를 4단계로 되돌렸다: **성별(200) → 초성(300) → 반(500) → 전체 공개(1000)**.
  (2026-07-30 순서 조정 — 성별을 먼저 연다. 초성은 후보를 몇 명으로 좁혀버려서
  먼저 팔면 그다음 단계를 살 이유가 약해진다. 요금은 그대로 두었다.)
- `my_vote_received` 뷰에 `voter_gender` 를 추가했다. 1단계를 산 뒤에만 나온다.
- 수집 항목이 늘었으므로 **개인정보처리방침에 성별을 명시**한다. "닉네임 + 성별 +
  학교 + 반" 조합은 개인 특정 가능성이 더 높아진다 — 소규모 클로즈드 테스트라는
  전제가 그만큼 더 중요해졌다.
- `gender` 컬럼은 nullable 로 둔다. NOT NULL 로 바꾸려면 성별을 받기 전에 만들어진
  기존 행을 먼저 채워야 하는데, 실제 사람의 성별을 임의로 지어낼 수는 없다.
  강제는 가입 함수에서 한다.
- 성별을 받기 전에 가입한 계정은 값이 비어 있다. 그 사람이 뽑은 투표에 GENDER 힌트를
  사면 빈 값이 나가므로, 클로즈드 테스트 전에 초기화하고 다시 가입받는다.

---

## 2026-07-29 · 학생용 MVP를 만들고, 검증은 성인이 한다

**결정** — 서비스의 내용은 **학생용**으로 만든다(질문·화면 맥락 모두).
다만 실제 이용자는 여전히 성인 지인이며, 그들이 학생용 MVP가 작동하는지 확인한다.
가입 화면과 개인정보처리방침에 **만 14세 미만 가입 금지**를 명시한다.

**이유** — 원 서비스가 중·고등학생 대상이고, 만들려는 것도 그것이다. 성인 맥락의
질문으로는 "학생이 쓸 때 말이 되는가"를 확인할 수 없다. 반대로 실제 학생을 받으면
법정대리인 동의와 모니터링이 필요해 감당할 수 없다. 내용은 학생용, 이용자는 성인으로
나누는 것이 두 문제를 동시에 푸는 방법이다.

**대안** — 성인 맥락 질문으로 클로즈드 테스트. 기각. 그렇게 하면 검증한 것이
만들려는 것과 달라진다.

**영향**
- 질문 세트를 학교 맥락으로 교체했다(`db/seed_questions.sql`). 외모·신체와
  부정형 질문을 뺀 원칙은 그대로다 — 학생 대상일수록 더 중요하다.
- 만 14세 미만 금지는 **자기 신고**다. 생년월일을 받지 않으므로 검증할 수단이 없고,
  받는 순간 개인정보 수집이 된다. 고지로 갈음하는 것이 이 설계의 일관된 선택이다.
- 확정 제약 "미성년자 유입 차단 — 성인 대상만"을 "이용자는 성인, 내용은 학생용,
  만 14세 미만 가입 금지"로 바꿨다.

---

## 2026-07-29 · NEIS 연동 후 테스터는 실제 학교 중 하나를 고른다

**결정** — P3 에서 실제 학교 목록이 들어오면 임시 조직("코드잇 DA 14기")을 대체한다.
성인 테스터는 다니지 않는 실제 학교를 골라 쓴다. 이는 문제되지 않는다.

**이유** — 소속은 후보를 나누는 기준일 뿐이고, 이 서비스는 재학 여부를 증명하지도
주장하지도 않는다. 임시 조직을 계속 끌고 가면 학교 맥락 화면(급식·시간표·공지, W8)을
검증할 수 없다 — 그 조직에는 급식도 시간표도 존재하지 않기 때문이다.

**영향**
- 온보딩 화면은 손대지 않아도 된다. 목록에서 고르는 구조가 그대로다.
- 테스터들이 같은 학교를 골라야 SCHOOL 스코프 후보가 모인다. 클로즈드 테스트
  안내에 "어느 학교를 고를지" 하나로 맞춰 알린다.
- 임시 조직은 P3 시점에 지운다.

---

## 2026-07-30 · 후보가 모자라면 스코프를 낮추지 않고 다른 친구로 채운다

**결정** — 스코프 안에서 후보 4명을 못 채우면, **스코프는 그대로 두고 친구 중
다른 사람으로 빈 자리를 채운다.** 채운 인원 수는 `vote_item.padded_count` 에
남기고, 화면에도 "이 범위의 친구가 모자라 다른 친구 N명이 후보에 들어갔습니다"
라고 밝힌다. 친구 전체로도 4명이 안 되면 그 질문은 내지 않는다.

**이유** — 하루 전 결정(스코프 하향)은 **모두가 같은 학교를 쓴다**는 전제 위에
있었다. 실제 학교 목록이 들어오면서 이용자가 여러 학교로 흩어지면, CLASS·SCHOOL
질문이 전부 GLOBAL 로 내려가 세 스코프를 나눈 의미가 사라진다.
채우는 방식은 스코프를 지키면서 질문을 살린다.

**대안** — 후보를 3명만 보여주기. 기각. 선택지 수가 들쭉날쭉하면 비교가 안 되고,
"4명 중 1명"이라는 전제로 만든 분석이 흔들린다.

**영향**
- 정합성 검사 두 개를 고쳤다. "CLASS 스코프에 타반 후보"는 이제 결함이 아니다.
  **"채운 수보다 타반 후보가 많다"** 를 결함으로 본다. 규칙대로 채운 것과
  규칙을 어긴 것을 구분할 수 있어야 하기 때문이다.
- `padded_count` 로 "친구 구성이 얼마나 편중돼 있나"를 셀 수 있다.
  같은 반 친구가 없을수록 이 값이 커진다.
- `effective_scope()` 함수는 지웠다. 남겨두면 어느 규칙이 진짜인지 헷갈린다.
- ⚠️ 이 값이 큰 투표는 "우리 반에서"라는 질문에 다른 반 사람이 섞인 결과다.
  분석할 때 `padded_count = 0` 인 것만 보는 편이 정확한 지표가 되는 경우가 있다.

---

## ~~2026-07-29 · 후보가 4명이 안 되면 스코프를 낮추고, 그래도 안 되면 질문을 내지 않는다~~ *(대체됨 → 위 항목)*

**결정** — 출제할 때 후보를 세어 4명이 안 되면 `CLASS → SCHOOL → GLOBAL` 로 한 단계씩
낮춘다. GLOBAL 에서도 모자라면 **그 질문은 출제하지 않는다**(세션에 넣지 않는다).
실제로 쓴 스코프는 `vote_item.candidate_scope` 에 남긴다.

**이유** — 친구 5명 게이트는 GLOBAL 후보만 보장한다. 친구가 5명이어도 같은 반 친구가
2명뿐이면 CLASS 질문을 낼 수 없다. 후보를 3명만 보여주거나 모르는 사람으로 채우는 것은
둘 다 나쁘다 — 전자는 선택지를 왜곡하고, 후자는 "세 스코프 모두 친구가 전제"라는
설계를 깬다.

**대안** — 후보 풀이 부족한 질문을 아예 질문 목록에서 빼기. 기각. 친구 구성은
사람마다 다르고 계속 바뀌므로, 질문 쪽이 아니라 출제 시점에 판단해야 한다.

**영향**
- 스코프를 낮춘 사실이 데이터에 남는다. `question.scope <> vote_item.candidate_scope`
  인 비율을 세면 "친구 구성이 얼마나 편중돼 있나"를 관찰할 수 있다.
- 질문을 건너뛰므로 세션의 문항 수가 10개보다 적을 수 있다. `vote_session.item_count`
  는 실제로 만든 수를 담는다.
- 유저에게 보이는 "건너뜀"은 없다. 애초에 그 질문이 나오지 않을 뿐이다
  (`vote_item` 에 스킵 컬럼이 없는 것과 같은 맥락이다).

---

## 2026-07-29 · 친구 맺기는 초대 코드로만, 요청·수락 2단계로

**결정** — 상대를 지목하는 수단은 초대 코드 하나다. 닉네임 검색은 넣지 않는다.
맺어지는 절차는 요청 → 수락이며, `friend_request` 와 `friendship` 의 직접 쓰기는
클라이언트에서 회수하고 `send_friend_request` / `accept_friend_request` /
`reject_friend_request` RPC 로만 처리한다.

**이유** — `app_user.id` 는 1부터 이어지는 정수다. `friend_request` INSERT 가
열려 있으면 `receiver_id` 를 1,2,3… 으로 바꿔가며 전체 가입자에게 요청을 뿌릴 수
있다. "코드를 아는 사람만 서로를 찾는다"는 전제가 통째로 무너진다.
수락 단계를 두는 이유는 잘못 입력한 코드나 원치 않는 상대가 조용히 친구가 되지
않게 하기 위해서다.

**대안** — 닉네임 검색. 기각. 닉네임에 UNIQUE 가 없어 동명이인을 구분하려면
소속을 함께 보여줘야 하는데, 그러면 "닉네임 + 학교 + 반"이 조회 가능한 명부가 된다.
개인정보를 받지 않기로 한 설계를 우회로 되돌리는 셈이다. 구 서비스 차단 사유 1위가
"모르는 사람임"(49.5%)이었던 것도 근거다.

**영향**
- 서로 코드를 주고받으면(양쪽 다 PENDING 요청을 만들면) 수락을 기다리지 않고
  그 자리에서 맺어진다. 둘 다 의사를 밝힌 상태이기 때문이다.
- 5명 게이트(`service_unlocked_at`)는 **수락 시점**에 찍는다. 보낸 것만으로 열리면
  혼자 계정을 만들어 통과할 수 있다.
- `friend_count` 는 증감이 아니라 `friendship` 을 세서 덮어쓴다(`refresh_friend_state`).
  구 서비스의 친구 수는 증감식이라 실제 관계와 어긋나 있었다.
- 거절당한 뒤 다시 보내면 요청이 되살아난다. 오타로 거절한 상대를 영영 다시 부를 수
  없게 만들지 않기 위해서다.
- 요청 목록에는 `my_friend_request` 뷰가 필요했다. 아직 친구가 아닌 상대의 닉네임을
  보여줘야 하는데 `app_user` 도 `friend_profile` 도 그 사람을 보여주지 않는다.

---

## 2026-07-30 · 테스트 조직은 이름을 유지하고 실제 학교의 정보를 빌려 쓴다

**결정** — 클로즈드 테스트 조직의 표시명은 "코드잇 DA 14기"로 두고,
급식·시간표·학사일정은 **서울고등학교**(표준학교코드 7010083)의 공개 데이터를 쓴다.
학급도 서울고의 2026학년도 실제 학급(1~3학년 × 1~14반)을 그대로 넣었다.

**이유** — 임시 조직에는 급식도 시간표도 없어서 W8 화면을 시험할 수 없다.
그렇다고 이름을 실제 학교로 바꾸면 테스터에게 낯설고, 앞서 계획했던 "임시 조직을
지우고 계정을 옮기는" 작업도 필요해진다. 표시명과 정보 출처를 분리하면 둘 다 없어진다.

**대안** — 조직 행의 `neis_school_code` 에 서울고 코드를 직접 넣기. 기각.
그 컬럼은 UNIQUE 라 서울고 행과 충돌하고, 학교 목록을 다시 받을 때
`ON CONFLICT` 가 조직 이름을 "서울고등학교"로 덮어쓴다.
→ `school.info_school_id` 를 추가해 가리키게 했다(migration 002).

**영향**
- 기존 1~4팀은 서울고 학급과 `(grade=1, class_num=1~4)` 가 겹쳐서 그대로 흡수됐다.
  3팀이던 계정들은 **1학년 3반**이 됐다. 계정 이전 작업이 없었다.
- 온보딩이 학년·반 두 단계로 바뀌었다. 반이 14개까지 있어 한 목록으로는 길다.
- 테스터에게 "1팀이면 1학년 1반"이라고 화면에서 안내한다.
- ⚠️ 보이는 급식·시간표는 **실제 서울고 학생들의 것**이다. W8 화면에
  출처를 밝히는 문구를 넣는다. 공개 데이터지만 "우리 반 시간표"로 읽히면 사실과 다르다.
- ⚠️ 분석할 때 이 조직은 실제 학교처럼 섞인다. `is_synthetic` 은 유저에만 있고
  학교에는 없다. P6 에서 학교별 지표를 만들 때 이 id(4)를 알고 있어야 한다.

---

## 2026-07-30 · 초대 링크는 동적 라우트 대신 쿼리스트링으로 만든다

**결정** — 초대 링크 주소를 `/add/<코드>` 가 아니라 **`/add?code=<코드>`** 로 한다.

**이유** — 이 개발 환경(Windows + Next 16)에서는 **동적 라우트가 개발 서버에서 열리지
않는다.** 내용을 지운 최소한의 `/probe/[x]` 로도 재현된다 — HTTP 500,
`Jest worker encountered 2 child process exceptions`. 정적 라우트는 멀쩡하고
`next build` 도 통과하므로 프로덕션에서는 동작할 가능성이 높지만,
**로컬에서 확인할 수 없는 구조를 서비스 경로에 두지 않는다.**
확인할 수 없으면 고칠 수도 없다.

**대안** — 동적 라우트를 쓰고 프로덕션에서만 시험. 기각. 배포해야만 확인되는 코드는
고치는 속도가 몇 배로 느려지고, 실패가 이용자에게 먼저 보인다.

**영향**
- 주소가 조금 길어진다. 카톡으로 주고받는 데는 차이가 없다.
- 앱 전체에 동적 라우트가 하나도 없다. 모든 라우트가 정적으로 빌드된다.
- `useSearchParams` 는 Suspense 경계가 필요해 페이지를 감쌌다.
- 원인이 환경에 있으므로, 개발 환경이 바뀌면 다시 볼 수 있다. 그때도 이득은
  주소 모양뿐이다.

---

## 2026-07-29 · 초대 링크는 배포(W7) 이후로 미룬다 *(해소됨 → 위 항목)*

**결정** — `/add/[코드]` 라우트와 "링크 복사" 버튼을 W4 에서 걷어냈다.
친구 추가는 코드 입력 하나로 한다.

**이유** — 주소가 `localhost:3000` 인 동안 링크는 친구 기기에서 열리지 않는다.
실사용 가치가 0인데, 개발 서버에서 이 라우트가 렌더링 워커째 죽는 문제
(`Jest worker encountered 2 child process exceptions`)가 있어 디버깅 비용만 남았다.
`next build` 는 통과하는데 dev 런타임에서만 터져 원인 추적에 시간이 든다.

**영향**
- W7 에서 실제 도메인이 생길 때 다시 만든다. 그때가 링크가 주 경로가 되는 시점이다
  (카톡으로 던지면 눌러서 바로 들어온다).
- 코드 입력칸은 주소나 군더더기가 섞여 들어와도 코드만 뽑아내도록 남겨두었다
  (`normalizeCode`). 링크가 없어도 "코드: XXXX" 같은 붙여넣기가 흔하다.
- ⚠️ 다시 만들 때 위 dev 에러부터 재현·해결하고 시작할 것.

---

## 2026-07-29 · 가입은 RPC 하나로만 한다

**결정** — 온보딩에서 `app_user` 를 클라이언트가 직접 INSERT 하지 않는다.
`complete_onboarding(닉네임, 반, 성별)` 함수로만 계정을 만들고,
`authenticated` 의 INSERT 권한 자체를 회수했다.

**이유** — INSERT 를 열면 같은 문장에 `heart_balance = 999999` 나
`is_synthetic = true` 를 끼워 넣을 수 있다. 하트는 "RPC 로만 바뀐다"고 정한 값이고,
`is_synthetic` 이 오염되면 BigQuery 에서 실유저와 합성 데이터를 영영 구분하지 못한다.
RLS 는 행 단위라 이걸 막지 못한다.

**대안** — 컬럼 단위 INSERT 권한(`GRANT INSERT (nickname, class_id, ...)`). 기각.
`invite_code` 가 NOT NULL UNIQUE 라 클라이언트가 코드를 만들어야 하고,
그러면 코드 생성 규칙과 중복 재시도가 브라우저로 나간다. 어차피 SQL 을 손대야 한다면
RPC 쪽이 화면 코드가 더 단순해진다.

**영향**
- 가입 하트 300 지급을 이 함수 안에서 함께 처리한다. 잔액을 만드는 트랜잭션이
  원장(`heart_transaction`)도 같이 만들어서, 구 시스템 최대 결함이었던
  원장·잔액 불일치가 생길 자리를 없앴다. 금액 근거는 구 서비스 실측 최빈값
  (`generator/config/distribution.yaml` · `signup_grant`).
- 두 번 호출되면 기존 행을 그대로 돌려준다. 새로고침·중복 클릭에 계정이 갈라지지 않는다.
- `policies.sql` 의 `insert_own_user` 정책은 제거했다.
- 정책·시드처럼 나중에 하나씩 추가되는 SQL 을 올리려고 `db/run_sql.py` 를 만들었다.

---

## 2026-07-30 · 증분 워터마크를 `updated_at` 하나로 통일한다

**결정** — 증분 적재 대상 30개 테이블에 `updated_at` 컬럼과 갱신 트리거를 심었다
(`db/migrations/004_updated_at_watermark.sql`). 적재 조건은 테이블과 무관하게
`WHERE updated_at > 지난 워터마크` 한 줄이다.

**이유** — 이 스키마의 시간 컬럼은 이름도 의미도 제각각이다.
`created_at` / `served_at` / `started_at` / `synced_at` 이 섞여 있고,
`vote_candidate` 와 `meal_menu_item` 은 시간 컬럼이 아예 없다.

더 위험한 것은 **행이 생성된 뒤에 바뀌는 값**이다. `vote_received.reveal_status` 는
힌트를 살 때마다 바뀌지만 그 변경을 기록하는 시간 컬럼이 없다. `created_at` 으로
증분을 뜨면 이 변경이 BigQuery 에 영영 도달하지 않는다. 그리고 이런 종류의 결함은
**오류를 내지 않는다** — 쿼리는 멀쩡히 답을 주고, 그 답이 틀렸다는 걸 알 방법이 없다.

**대안**
- 테이블마다 `greatest(created_at, coalesce(read_at, ...))` 식으로 워터마크 식을 쓴다.
  기각. `reveal_status` 처럼 **아무 시각도 남기지 않는 변경**을 여전히 놓치고,
  테이블이 늘어날 때마다 식을 손으로 맞춰야 한다.
- 전부 매번 통째로 다시 적재한다. 실유저 3.5만 행만 보면 이게 제일 싸지만,
  합성 786만 행이 같은 파이프라인을 타므로 성립하지 않는다.

**영향**
- 워터마크 컬럼에 인덱스를 함께 만들었다. 없으면 "증분"이라면서 매번 전체를 훑는다.
- 마스터·참조 테이블 12개는 대상이 아니다. 가장 큰 `school` 도 5,725행이라
  통째로 교체가 싸다. 어느 쪽인지는 `pipeline/tables.yaml` 이 정한다.
- 갱신이 실제로 잡히는지 시험했다 — 더미 유저 한 명을 UPDATE 하고 증분을 돌려
  BigQuery 에서 **1행이 중복 없이 갱신**되는 것을 확인했다.

---

## 2026-07-30 · 실유저와 합성 데이터를 같은 BigQuery 테이블에 `_source` 로 섞는다

**결정** — 원천이 둘(Supabase 실유저 / 로컬 합성)이지만 BigQuery 에서는 같은
테이블 하나다. 적재할 때 `_source` 컬럼을 붙이고, 키를 `(_source, id)` 로 쓴다.

**이유** — 둘 다 `id` 가 1부터 이어지는 identity 다. 그대로 섞으면 실유저 1번과
합성 1번이 **서로를 덮어쓴다.** 테이블을 나누면 이 문제는 없지만 분석 쿼리가
전부 두 벌이 되고, "합성으로 검증한 쿼리를 실데이터에 그대로 돌린다"는
이 프로젝트의 전제가 깨진다.

**대안** — `app_user.is_synthetic` 플래그로 구분. 기각. 그 컬럼은 `app_user` 에만
있어서 나머지 41개 테이블에는 쓸 수 없고, id 충돌도 해결하지 못한다.

**영향**
- 분석에서 실유저만 보려면 `WHERE _source = 'supabase'` 다.
- 워터마크도 원천별로 따로 관리한다 (`raw._load_state`).
- `_loaded_at` 도 함께 남긴다. 언제 들어온 행인지가 파이프라인 자체의 분석 대상이다.

---

## 2026-07-30 · 대량 적재 후 `updated_at` 을 각 행의 원래 시각으로 되돌린다

**결정** — `db/ddl/96_backfill_updated_at.sql` 을 95(시퀀스 재동기화)와 같은 자리에
두고, 대량 적재 직후 함께 돌린다.

**이유** — `updated_at` 의 기본값은 `now()` 다. COPY 로 786만 행을 부어 넣으면
그 786만 행이 전부 "적재한 순간"이 된다. 3개월치 데이터가 하루에 뭉친다.

BigQuery raw 테이블은 `updated_at` 으로 파티션을 나눈다. 전부 한 파티션에 들어가면
파티션 가지치기가 통째로 무력해지고 쿼리마다 786만 행을 다 읽는다. 무료 등급
(월 1TB)에서 이건 실제로 아픈 비용이다.

**대안** — 파티션 기준을 `created_at` 으로 잡는다. 기각. `created_at` 이 없는
테이블이 있고(`vote_candidate`), 그러면 파티션 컬럼이 테이블마다 달라져
004 로 통일한 것이 도로 흩어진다.

**영향**
- 되돌릴 때 트리거를 껐다 켠다. 켜둔 채 UPDATE 하면 트리거가 방금 넣은 값을 다시
  `now()` 로 덮어써서, 아무 일도 하지 않은 것처럼 끝난다.
- `vote_candidate`(379만 행)와 `meal_menu_item` 은 자기 시각이 없어 부모의 시각을
  물려받는다. 후보는 출제와 동시에 만들어지므로 `vote_item.served_at` 이 실제 생성 시각이다.
- 원래부터 `updated_at` 이 있던 4개 테이블(`app_user`·`school`·`post`·`post_comment`)은
  건드리지 않는다. 그 값은 **진짜 수정 이력**이라 `created_at` 으로 덮으면 데이터를 잃는다.

---

## 2026-07-30 · BigQuery 에 GCS 를 경유하지 않고 직접 올린다

**결정** — 추출한 행을 GCS 버킷에 내려놓지 않고 JSON 으로 곧장 올린다
(`load_table_from_json`). `GCS_BUCKET` 은 비워 둔다.

**이유** — GCS 경유는 대용량 적재의 정석이지만, 버킷·권한·수명주기 설정이
늘어나는 값을 이 규모가 정당화하지 못한다. raw 전체가 **450MB** 다.
`google-cloud-storage` 와 `pyarrow` 를 requirements 에서 뺐다.

**대안** — GCS 를 거치는 표준 경로. 기각. 데이터가 수십 GB 로 커지면
그때 다시 본다. 지금은 중간 저장소가 시간을 줄여주지 않는다.

**영향**
- 적재는 배치(5만 행)로 나눠 올린다. 786만 행을 한 덩어리로 만들면 메모리가 터진다.
- 서비스 계정 키(`credentials.json`)는 커밋하지 않는다. Airflow 컨테이너에는
  프로젝트 폴더 마운트로 들어간다 — 그래서 이 컴포즈는 로컬 전용이다.

**정정 (2026-07-30)** — 처음에는 "결제 계정 없이 샌드박스로 시작한다"고 적었으나
사실이 아니었다. 데이터셋과 테이블의 만료 설정을 조회하니 전부 비어 있었다.
샌드박스라면 데이터셋 생성 시 **60일 만료가 강제로** 걸리므로, 첫 적재 시점에
이미 결제 계정이 붙어 있었다는 뜻이다. 60일 만료 제약은 없다.
대신 무료 한도(저장 10GiB · 쿼리 1TiB/월)를 넘으면 막히지 않고 과금되므로
예산 알림을 걸어 둔다.

---

## 2026-07-30 · Airflow 는 공식 컴포즈 대신 2개 서비스로 줄인다

**결정** — 공식 배포판 `docker-compose.yaml`(서비스 8개)을 쓰지 않고,
`standalone` + 메타DB 두 개만 띄운다 (`airflow/docker-compose.yml`).

**이유** — 공식 구성은 CeleryExecutor 를 전제한다(redis · worker · flower · triggerer).
**하루 한 번 42개 테이블을 옮기는 일**에 워커 큐는 필요 없고, 작업자가 개발
경험이 없어서 서비스가 8개면 어디가 죽었는지 판단할 수가 없다.

**대안** — SQLite 메타DB 로 더 줄인다. 기각. SequentialExecutor 로 묶여
병렬 실행이 아예 막힌다. Postgres 컨테이너 하나 더 띄우는 값이 그보다 싸다.

**영향**
- 적재 로직을 DAG 안에 두지 않고 `pipeline/` 의 스크립트를 부른다.
  Airflow 없이 같은 명령으로 돌릴 수 있어야 디버깅이 된다.
- 합성 데이터는 스케줄에 넣지 않는다. 한 번 만들어지고 변하지 않는다.
- 적재 뒤에 반드시 행 수를 대조한다(`verify_load.py`). 증분은 조용히 틀리기 때문에
  "오류가 안 났다"는 것이 성공의 증거가 되지 못한다.

---

## 2026-07-30 · 원천의 삭제는 BigQuery 로 전파하지 않는다

**결정** — 증분 MERGE 는 INSERT 와 UPDATE 만 한다. Postgres 에서 행이 지워져도
BigQuery raw 에는 남는다.

**이유** — raw 층의 일은 **원천에서 일어난 일을 잃지 않는 것**이다. 탈퇴나
`db/reset_users.py` 로 사라진 유저도 그때까지의 활동은 분석 대상이다.
삭제까지 따라가면 "왜 그만뒀는가"를 물을 데이터 자체가 없어진다.

**대안** — 원천에 없는 행을 지우는 `WHEN NOT MATCHED BY SOURCE THEN DELETE`.
기각. 증분으로 뽑은 일부 행만 들고 이 판단을 하면 멀쩡한 행을 지운다.

**영향** — `verify_load.py` 가 부족(`pg > bq`)과 잉여(`pg < bq`)를 구분해서
보고한다. 부족은 결함이고, 잉여는 삭제 이력이라 정상이다.

---

## 2026-07-30 · 워터마크를 스냅샷보다 5분 뒤로 물려 저장한다

**결정** — 증분 적재를 마친 뒤 워터마크를 `스냅샷 시각 - 5분` 으로 기록한다.
매 실행이 최근 5분치를 다시 읽는다.

**이유** — 워터마크 경합 때문이다. `updated_at` 은 트리거가 `now()` 로 채우는데,
Postgres 의 `now()` 는 **트랜잭션이 시작한 시각**이다.

쓰기 트랜잭션 W 가 09:00:00 에 시작해 `updated_at = 09:00:00` 을 박고
09:00:02 에 커밋한다고 하자. 적재가 09:00:01 에 스냅샷을 뜨면 W 는 아직 커밋
전이라 **보이지 않는다.** 그런데 워터마크는 09:00:01 로 전진한다. 다음 적재는
`updated_at > 09:00:01` 을 뜨므로 W 의 행은 **영원히 걸리지 않는다.**

확률은 낮다(이 앱의 쓰기는 RPC 하나, 밀리초 단위다). 그러나 이런 누락은
오류를 내지 않는다 — 쿼리는 멀쩡히 답을 주고, 그 답이 틀렸다는 걸 알 방법이 없다.
`verify_load.py` 가 행 수로 잡아주긴 하지만, **갱신 누락은 행 수로도 안 잡힌다.**

**대안**
- 트랜잭션 ID(`pg_current_snapshot`)로 추적한다. 정확하지만 원천마다 상태를
  더 들고 다녀야 하고, 이 규모에서 얻는 게 없다.
- 무시한다. 기각. 5분 재읽기의 값이 훨씬 싸다 — MERGE 는 멱등이라
  같은 행을 몇 번 읽어도 결과가 같고, 5분치는 실유저 몇 행 수준이다.

**영향** — 매 실행이 최근 5분치를 다시 읽는다. 합성 786만 행처럼 한 번 만들고
안 바뀌는 원천에서는 재읽기가 0 이다.

---

## 2026-07-30 · 로컬 DB 는 `apply.py` 로만 만든다

**결정** — README 의 "스키마 만들기"를 `cat db/ddl/*.sql | psql` 에서
`python db/apply.py --target local` 로 바꿨다.

**이유** — 그 `cat` 경로에는 `db/migrations/` 가 없다. 실제로 로컬 합성 DB 가
마이그레이션 3종(001·002·003) 뒤처진 채 몇 주를 돌았고, 그 결과:

- 정합성 검사 17종이 `padded_count` 를 못 찾아 **통째로 실행되지 않았다.**
  합성 데이터를 검증할 수단이 끊겨 있었는데 아무도 몰랐다.
- 그 상태로 BigQuery 에 올라가 `vote_item.padded_count` 가 합성 803,187행
  전부 NULL 이 됐다.

문서가 두 개의 서로 다른 재구축 경로를 말하고 있었던 것이 원인이다
(CLAUDE.md 는 `apply.py`, README 는 `cat`). 진실을 하나로 줄였다.

**영향**
- 마이그레이션을 적용한 뒤에는 그 테이블을 `--full-refresh` 해야 한다.
  `ALTER TABLE ADD COLUMN` 은 트리거를 발동시키지 않아 `updated_at` 이 그대로고,
  증분은 아무것도 잡지 못한다. 이 함정을 CLAUDE.md 에 적었다.
- `docker run` 에 `--shm-size=1g` 를 추가했다. 도커 기본 64MB 로는 786만 행
  정합성 검사가 `No space left on device` 로 죽는다. 디스크가 아니라 공유메모리다.

---

## 2026-07-30 · 두 원천의 id 가 겹친다 — 조인에 `_source` 를 강제한다

**결정** — 당장은 규칙으로 못 박고(`WHERE`·`JOIN` 에 `_source` 필수),
P6 stg 층에서 대리키를 만들어 구조적으로 막는다.

**이유** — 실측했더니 `app_user` 는 16개, `vote_item` 은 26개 id 가 두 원천에
동시에 존재한다. 적재는 키가 `(_source, id)` 라 안전하지만, **분석 쿼리는
아무도 지켜주지 않는다.** `JOIN ... USING(id)` 한 줄이면 실유저의 투표에
합성 유저의 프로필이 붙고, 그래도 오류는 나지 않는다.

**대안** — 원천별로 테이블을 나눈다. 기각. 분석 쿼리가 전부 두 벌이 되고,
"합성으로 검증한 쿼리를 실데이터에 그대로 돌린다"는 전제가 깨진다.

**영향** — P6 의 첫 작업은 stg 층에서 `_source || '-' || id` 형태의 대리키를
만드는 것이다. raw 를 직접 조인하는 쿼리는 임시로만 쓴다.

---

## 2026-07-30 · 자유게시판을 연다 — 글도 댓글도 닉네임으로

**결정** — 게시판을 **닉네임이 드러나는 형태**로 열었다(W9). 글쓴이뿐 아니라
**댓글 작성자도 닉네임**이다. 스키마에 있던 익명 번호(`post_comment.anonymous_seq`)는
쓰지 않고 nullable 로 바꿨다(마이그레이션 005).

**이유** — 익명 게시판을 v1 에서 뺀 이유는 "사고가 나도 책임을 물을 수 없다"였다.
글쓴이가 붙으면 그 전제가 바뀐다. 그런데 **댓글만 익명으로 두면 그 구멍이 그대로
남는다.** 사고는 오히려 댓글에서 잦다 — 글은 쓰기 전에 한 번 생각하지만 댓글은 안 그렇다.
반쪽만 책임지는 구조를 만들 이유가 없었다.

**대안**
- 글은 닉네임, 댓글은 익명 번호(스키마 원안). 기각. 위 이유.
- 글·댓글 모두 익명. 기각. v1 에서 빼기로 한 바로 그것이고, 재검토 조건
  (신고 검토 인력)이 갖춰지지 않았다.

**영향**
- `anonymous_seq` 를 지우지 않고 **nullable** 로만 바꿨다. 익명 게시판은 폐기가
  아니라 보류다. 지웠다 다시 만들면 그 사이 댓글에 번호를 소급할 수 없다.
- 부수 효과가 하나 있었는데 이게 더 중요하다 —
  `UNIQUE (post_id, author_id, anonymous_seq)` 때문에 번호를 1 로 고정하면
  **한 사람이 한 글에 댓글을 두 번 달 수 없다.** 대화가 성립하지 않는다.
  nullable 로 바뀌면서 함께 풀렸다(Postgres 의 UNIQUE 는 NULL 을 서로 다르게 본다).
- 목록에 남의 닉네임을 띄워야 하는데 `app_user` 는 본인 행만 읽힌다.
  정책을 넓히지 않고 `board_post`·`board_comment` 뷰로 필요한 컬럼만 냈다.
  정책을 넓혔으면 하트 잔액까지 따라 나갔을 것이다.

---

## 2026-07-30 · 게시판 범위는 학교, 카테고리는 하나

**결정** — `post.school_id` 는 클라이언트가 정하지 않고 RPC 가 글쓴이의 소속에서
끌어온다. 카테고리는 시드에 5개가 있지만 **자유게시판 하나만** 연다.

**이유** — 범위를 학교로 잡은 것은 스키마가 그렇게 설계돼 있어서이기도 하지만,
"누구와 같이 있는가"가 게시판의 전제이기 때문이다.
⚠️ 이때 `my_info_school_id()` 를 쓰면 안 된다. 그건 급식·시간표를 **어느 학교에서
빌려오는가**이고, 코드잇 DA 14기 사람이 서울고 게시판을 보게 된다.

카테고리를 하나로 둔 것은 규모 때문이다. 테스터가 20~50명인데 5개로 나누면
각 게시판이 텅 빈다. **빈 게시판은 아무도 쓰지 않는다.** 글이 쌓이는 걸 보고 쪼갠다.

**대안** — 반(class) 단위 게시판. 기각. 학교보다 더 작아서 같은 문제가 심해진다.

**영향** — 카테고리 코드는 `create_post` 의 인자로 남겨뒀다(기본값 `FREE`).
나중에 열 때 함수 시그니처를 바꾸지 않아도 된다.

---

## 2026-07-30 · 신고는 게시판과 함께, 차단은 뒤로

**결정** — 글·댓글 신고를 게시판과 같이 냈다. 차단(`block_record`)은 만들지 않았다.

**이유** — "게시판을 열기 전에 차단·신고 화면이 먼저"라고 적어뒀지만, 둘의 급함이
다르다. **게시판은 공개 공간이라 "안 보이게"보다 "내려가게"가 먼저 필요하다.**
차단은 상대가 나에게 직접 말을 걸 수 있을 때 — 즉 1:1 메시지가 생길 때 — 값이 생긴다.

**대안** — 차단까지 다 만들고 게시판을 연다. 기각하지 않고 **순서만 바꿨다.**
남은 두 기능(익명 메시지·채팅방) 전에는 차단이 있어야 한다.

**영향**
- 신고해도 **자동으로 내려가지 않는다.** 자동 숨김은 집단 신고에 취약해 진작에
  기각한 방식이다. 기록만 남고 사람이 판단한다 — 검토 인력이 없는 상태로 여는
  것이므로, 작업자가 `report` 테이블을 직접 봐야 한다.
- 같은 사람이 같은 대상을 여러 번 신고해도 한 건으로 센다. 안 막으면 한 사람이
  `report_count` 를 얼마든지 올릴 수 있다.
- 글 삭제는 행을 지우지 않고 `status = 'DELETED'` 로 둔다.
  `report.target_post_id` 가 FK 라 지우면 신고 기록이 가리킬 대상이 사라진다.
  **사고가 난 글일수록 지워지면 안 된다.**

---

## 2026-07-30 · 학교 경계는 기술로 막지만, 소속은 자기신고다

**결정** — 게시판의 학교 경계를 읽기·쓰기 **모든 경로**에서 막았다. 다만
"그 학교 사람인가"는 **검증하지 않는다.** 이 한계를 문서에 명시하고 그대로 간다.

**확인한 것** — 다른 학교 계정으로 14가지 경로를 실제로 뚫어봤다.
목록 조회 · 상세 · 댓글 목록 · `post`/`post_comment`/`post_like` 직접 조회 ·
글쓴이 닉네임 캐내기 · 댓글 달기 · 글/댓글 좋아요 · 신고 · 조회수 · 삭제.
**전부 막혔고**, 함수가 성공을 돌려주는 경우(신고·조회수·삭제)에도
글의 `report_count`·`view_count`·`status` 는 그대로였다. 시험으로 굳혔다(게시판 29종).

**막지 않은 것** — 유저는 자기 `class_id` 를 바꿀 수 있고(온보딩 수정),
다른 학교의 학급으로 옮기면 그 학교 게시판이 보인다.

**왜 막지 않는가** — 막아도 의미가 없다. 익명 계정은 접속만 하면 무제한으로
생기고, 온보딩에서 **아무 학교나 고를 수 있다.** `class_id` 변경을 잠가봐야
새 계정을 만들면 그만이다. 마찰만 늘고 막히는 것은 없다.

근본 원인은 **소속을 검증할 수단이 없다는 것**이고, 그건 이 프로젝트의 핵심 제약에서
직접 나온다 — 학교 이메일도 인증 코드도 받지 않기로 했다(개인정보를 받지 않는다).
검증하려면 그 결정을 뒤집어야 한다.

**대안**
- 학교 이메일 인증. 기각. 개인정보를 받지 않는다는 전제를 깬다.
- 같은 학교 사람의 초대가 있어야 그 학교에 들어갈 수 있게. 기각.
  첫 사람이 들어갈 방법이 없고, 클로즈드 테스트 규모에서 과하다.
- `class_id` 변경을 같은 학교 안으로 제한. 기각. 위 이유 — 새 계정을 막지 못한다.

**영향**
- 게시판은 **"같은 학교라고 밝힌 사람들끼리"** 보는 공간이다.
  기술적 격리는 성립하고, 신원 보증은 성립하지 않는다.
- 실질 위험은 낮다. 게시판이 있다는 것과 학교 이름을 알아야 찾아올 수 있고,
  지금은 성인 지인 대상 클로즈드 테스트다.
- 불특정 다수에게 열 때는 이 항목을 **먼저 다시 봐야 한다.** 그때는
  소속 검증이 없다는 것이 실제 문제가 된다.

---

## 2026-07-30 · 친구 추천 — "초대 코드로만"을 같은 학교 범위에서 연다

**결정** — 같은 학교 사람을 목록으로 보여주고, 코드 없이 친구 요청을 보낼 수 있게
했다(W10). "안 볼래"로 목록에서 뺄 수도 있다.

**이건 기존 결정을 뒤집는다** — "친구 맺기는 초대 코드로만"(2026-07-29)과
"id 로 남을 지목할 수 있는 경로를 만들지 않는다"를 좁은 범위에서 연다.
그래서 무엇을 얻고 무엇을 잃는지를 분명히 적어 둔다.

**이유** — 코드 교환이 실제로 걸림돌이다. 투표는 친구 5명이 있어야 열리는데,
지인 20~50명이 서로 코드를 주고받아 각자 5명을 채우려면 교환이 수십 번 일어난다.
한 사람이라도 빠지면 그 사람은 투표를 못 해보고 이탈한다.
**클로즈드 테스트에서 얻으려는 것이 "실제로 투표가 일어난 데이터"인데,
코드 교환의 마찰이 그 데이터를 못 만들게 하고 있었다.**

**범위를 좁히는 데 쓴 장치**

- **같은 학교만.** 다른 학교 사람은 목록에도 없고 `send_request_to()` 도 거부한다.
- **더미(`is_synthetic`)는 나오지 않는다.** 더미를 남겨둔 근거가
  "테스터가 마주칠 경로가 없다"였다. 추천에 뜨면 그 전제가 즉시 깨지고
  지인이 `시험친구01` 에게 요청을 보낸다. 시험 항목으로 굳혔다.
- **초대 코드를 내보내지 않는다.** 목록에 코드를 실으면 그 화면이 곧 코드 명부다.
  나가는 값은 닉네임과 학년·반뿐이다.
- **이미 오간 요청이 있으면 목록에서 뺀다.** 거절당한 사람이 다시 뜨면 코드 없이
  반복해서 부를 수 있다. 초대 코드 경로는 그대로 열려 있어 진짜 재시도는 가능하다.
- **`send_request_to()` 가 대상이 정말 추천 목록에 있는지 서버에서 다시 확인한다.**
  이게 이 기능의 안전장치 전부다. 클라이언트가 보낸 id 를 믿으면 이 함수가
  곧 전체 가입자 지목 통로가 된다.

**남는 위험** — 소속이 자기신고라(위 항목 참조) 아무나 어떤 학교에 들어가
그 학교 명단을 보고 요청을 뿌릴 수 있다. 지금은 성인 지인 클로즈드 테스트라
실질 위험이 낮지만, **불특정 다수에게 열 때는 이 기능을 먼저 다시 봐야 한다.**

**대안**
- 상호 친구가 있는 사람만 추천(`MUTUAL_FRIEND`). 기각 — 지금 풀려는 문제가
  "첫 친구를 어떻게 만드나"인데, 이 방식은 이미 친구가 있어야 작동한다.
- 같은 반으로만 좁힘. 기각 — 테스터가 여러 반에 흩어져 있어 대부분 빈 목록이 된다.
  대신 같은 반을 목록 위로 올린다.

**영향**
- 거절율을 잴 수 있게 됐다. "안 볼래"는 `friend_recommendation.dismissed_at` 에,
  보낸 요청은 `friend_request.source = 'RECOMMEND'` 에 남는다.
  초대 코드로 맺은 관계와 추천으로 맺은 관계를 나눠 볼 수 있다.
- **추천이 화면에 뜬 것 자체는 기록하지 않는다.** 매번 쓰기가 생기고,
  노출 대비 전환율은 이 규모에서 볼 값이 아니다. 필요해지면 그때 만든다.
- "안 볼래"는 차단이 아니다. 상대는 여전히 나를 추천받고 요청할 수 있다.
  차단은 별개 기능이고 아직 없다.

---

## 2026-07-30 · 삭제를 전파하지는 않되, 표시는 한다 (앞 항목 보완)

**결정** — "원천의 삭제는 BigQuery 로 전파하지 않는다"는 그대로다. 행은 지우지 않는다.
다만 **`_deleted_at` 을 찍어 지워진 행임을 표시한다.**

**이유** — 표시가 없으면 **지워진 행과 살아 있는 행을 분간할 수 없다.**
그날 실제로 물렸다. 시험 계정을 정리한 뒤 "오늘 들어온 사람"을 물었더니
이미 지운 계정 7개가 함께 나왔고, "오늘 투표한 수"는 지운 투표 20건을 세어
**실제 0건을 20건으로 보고했다.** 쿼리는 오류를 내지 않았다 — 이 프로젝트에서
반복해서 나오는 그 실패 방식이다.

앞 항목의 근거("탈퇴한 사람도 그때까지의 활동은 분석 대상이다")는 여전히 맞다.
틀린 것은 "지우지 않는다"가 아니라 **"지워졌다는 사실도 남기지 않는다"** 였다.
그 사실 자체가 분석 대상이다 — 언제 그만뒀는지가 리텐션이다.

**대안**
- 쿼리에서 그때그때 걸러낸다. 기각. 거를 방법이 없다(그게 문제였다).
  안다 해도 모든 쿼리가 그 지식을 기억해야 한다.
- 진짜로 지운다. 기각. 앞 항목 그대로 — "왜 그만뒀는가"를 물을 데이터가 사라진다.

**영향**
- 적재 후 원천의 PK 전부를 임시 테이블로 올려 대조한다.
  **실유저(supabase)에만 기본으로 돈다** — 합성 786만 개를 매번 올리는 값이
  얻는 것보다 크고, 재생성은 `--full-refresh` 라 유령이 애초에 안 생긴다.
  끄려면 `--no-mark-deleted`.
- ⚠️ **분석 쿼리에 `_deleted_at IS NULL` 이 거의 항상 필요하다.**
  `_source` 와 함께, raw 를 직접 조회할 때 잊으면 안 되는 두 번째 조건이다.
  P6 stg 층에서 이 둘을 미리 걸러 내보내면 실수할 자리가 사라진다.

---

## 2026-07-30 · 프로필 수정도 RPC 하나로 — 직접 UPDATE 권한 회수

**결정** — `update_profile()` 로만 닉네임·성별·소속을 바꾼다. W1 에서 열어둔
`GRANT UPDATE (nickname, class_id, gender) ON app_user` 를 회수했다.

**이유** — 그 권한 자체는 안전했다. 컬럼 단위로 제한돼 있어 `heart_balance` 나
`is_synthetic` 은 건드릴 수 없었다. 문제는 **검증이 없다**는 것이었다.

가입은 `complete_onboarding()` 이 닉네임 2~20자, 성별 필수, 학급 존재를
확인한다. 그런데 직접 UPDATE 는 그 규칙을 전부 우회했다.

| 우회 | 결과 |
|---|---|
| `nickname = ''` | 빈 이름으로 게시판에 글이 올라간다 |
| `gender = NULL` | 힌트 4단계 중 첫 단계(성별)가 사라진다 |
| `class_id` 아무거나 | 학급이 없는 학교로 옮겨 반을 못 고르는 상태가 된다 |

**가입과 수정이 같은 규칙을 따라야 한다.** 두 곳의 기준이 갈리면 어느 쪽이
맞는지 알 수 없다. 통로를 하나로 모으고 직접 권한은 닫았다 — 남겨두면
RPC 의 검증이 우회 가능해 있으나 마나가 된다.

**대안** — 직접 UPDATE 를 두고 트리거로 검증. 기각. 이 프로젝트는 "규칙이 있는
쓰기는 RPC 로"가 이미 확립된 규칙이고, 검증 위치를 둘로 나눌 이유가 없다.

**영향**
- 소속은 `selectable_school` 안으로 제한된다. FK 만 믿으면 전국 5,724개 학교
  아무 데나 갈 수 있는데, 학급이 없는 학교로 가면 반을 고를 수 없다.
- **학교를 옮기면 게시판과 친구 추천이 새 학교 기준으로 바뀐다.** 이미 쓴 글은
  옛 학교에 남고 본인도 더는 볼 수 없다. 글은 그 학교 사람들에게 남긴 것이지
  글쓴이를 따라다니는 것이 아니라고 보았다. 화면이 누르기 전에 이 사실을 알린다.
- **닉네임을 바꾸면 과거 글과 댓글에도 새 이름이 보인다.** `board_post` 뷰가
  `app_user` 를 그때그때 조인하기 때문이다. 이름 변경 이력은 남기지 않는다 —
  지금 필요하지 않고, 필요해지면 그때가 이력 테이블을 만들 때다.
- 화면은 온보딩 폼을 재사용한다. 학교 검색과 반 선택이 통째로 같아서, 복사하면
  학교 목록 규칙이 바뀔 때 한쪽만 고치는 사고가 난다.

---

## 2026-07-30 · 계정 삭제는 행을 지우지 않는다

**결정** — 본인이 계정을 삭제할 수 있게 하되, **행을 지우지 않고**
`status = 'WITHDRAWN'` 으로 바꾸고 사유를 `user_withdrawal` 에 남긴다.
`auth_user_id` 를 NULL 로 끊어 그 브라우저로는 다시 들어올 수 없게 한다.

**이유** — 구 서비스의 탈퇴 테이블은 70,764건(가입자의 10.5%)인데 유저 식별자가
없어 **누가 탈퇴했는지 특정할 수 없었다.** 사유의 57%가 "기타"였고, 탈퇴 사유를
유저 속성과 교차분석하는 것이 원천 봉쇄됐다. 그 구멍을 닫으려고
`user_withdrawal.user_id` 를 NOT NULL FK 로 두기로 이미 결정했다.
행을 지우면 그 설계가 통째로 무의미해진다.

**"언제 왜 그만뒀는가"는 이 프로젝트가 얻으려는 데이터 그 자체다.**

**대안**
- 물리 삭제. 기각. 위 이유. 개인정보를 애초에 받지 않으므로 남겨서 생기는
  법적 부담도 없다 — 남는 것은 닉네임·성별·소속뿐이다.
- 사유를 받지 않고 바로 삭제. 기각. 사유가 없으면 `user_withdrawal` 이
  구 서비스와 같은 상태(누가·왜가 없는 숫자)가 된다.

**영향**
- 이용자 입장에서 달라지는 것: 친구 목록·추천·투표 후보에서 사라지고
  (전부 `status = 'ACTIVE'` 를 본다), 그 브라우저로 다시 들어오면 새 계정이 된다.
- **게시판 글은 남기고 이름만 "탈퇴한 사용자"로 가린다.** 댓글이 달린 글을
  지우면 남의 대화까지 사라진다. `board_post`·`board_comment` 뷰에서 처리했다.
- ⚠️ 친구 관계(`friendship`)는 지우지 않는다. 지우면 상대의 `friend_count` 가
  5 아래로 떨어져 **남의 투표가 닫힌다.** 대신 상대의 friend_count 가 실제보다
  크게 남는다 — 게이트 판정에만 쓰이고 게이트는 한 번 열리면 유지되므로 영향이 없다.
- 화면은 두 단계다. 사유를 고르고, 확인 문구("삭제")를 직접 입력해야 버튼이 열린다.
  되돌릴 수 없는 동작이라 "실수로 눌렀다"가 나올 수 없어야 한다.

---

## 2026-07-30 · 화면 이동을 브라우저 이력에 싣는다

**결정** — 화면을 바꿀 때 `history.pushState` 로 한 칸 쌓고, `popstate` 에서
홈으로 되돌린다. 화면 안의 닫기 버튼도 `history.back()` 을 거친다.

**이유** — 이 앱은 주소를 나누지 않고 상태로 화면을 가른다(동적 라우트를 쓸 수
없는 사정은 [[CLAUDE]]). 그대로 두면 **휴대폰 뒤로가기가 앱을 나가버린다.**
테스터 대부분이 폰으로 들어오므로 실제로 자주 밟는 자리다.

**대안** — 화면마다 주소를 준다. 기각. 이 환경의 개발 서버가 동적 라우트를
열지 못해 로컬에서 확인할 수 없다. 확인할 수 없는 화면은 만들지 않는다.

**영향** — 닫기 버튼이 `setScreen("home")` 이 아니라 `history.back()` 을 부른다.
직접 상태만 바꾸면 쌓아둔 이력이 남아 뒤로가기를 두 번 눌러야 앱이 나가진다.

---

## 2026-07-30 · 하트 충전은 결제 없는 스텁 — 대신 하루 한 번

**결정** — 충전 화면을 열되 **실결제를 붙이지 않는다.** 누르면 그 자리에서
하트가 들어온다. 대신 **어떤 상품을 골랐든 하루에 한 번만** 받을 수 있다.

**이유** — 실결제(인앱결제)는 계정 개설과 심사가 필요해 MVP 일정 밖이다
(2026-07-29 결정 그대로다). 그런데 **하트 소비처만 있고 공급처가 없으면
하트 경제를 관찰할 수 없다.** 힌트를 몇 번 사면 잔액이 마르고, 그 뒤로는
아무 일도 일어나지 않는다. 실제로 김형민의 잔액이 1,300에서 18까지 내려갔다.

결제가 없으므로 막지 않으면 하트는 무한이 된다. 무한이면 힌트 가격도
소비 패턴도 의미가 없어진다 — 이 프로젝트가 보려는 것이 그것이다.
**하루 한 번이 그 자리를 대신하는 유일한 장치다.**

**대안**
- 충전을 계속 제외. 기각. 잔액이 마르면 받은 투표 화면이 죽은 화면이 된다.
- 상품마다 따로 하루 한 번. 기각. 네 상품을 다 누르면 5,977하트가 되어
  제한이 없는 것과 같다.
- 광고 시청 보상. 기각. 광고 자체가 스텁이라 같은 문제가 한 겹 늘어날 뿐이다.

**영향**
- ⚠️ **가짜 매출이 진짜처럼 쌓인다.** `heart_purchase` 에 SUCCESS 로 들어가기
  때문이다. `store_transaction_id` 에 `MVP-STUB-` 접두어를 남겨 구분한다.
  **매출을 셀 때 반드시 거른다.** 안 남기면 나중에 실결제가 섞였을 때
  무엇이 진짜였는지 되짚을 방법이 없다.
- 날짜 판정은 **DB 가 한다**(`my_topup_state` 뷰, 한국 시간 기준).
  화면에 두면 브라우저 시간대를 바꾸는 것만으로 제한이 뚫린다.
- 잔액과 원장을 한 문장 간격 안에서 같이 쓴다. 구 시스템 최대 결함이
  원장에 남지 않은 충전이었다 — 그 자리를 만들지 않는다.
- 라벨은 화면용이라 사진에 맞췄다(heart.1000 "최고 인기", heart.4000
  "최고 가성비"). 금액과 수량은 구 서비스 실측이라 건드리지 않았다.
