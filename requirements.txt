# =====================================================================
# requirements.txt · 로컬 스크립트용 (합성 데이터 생성 / 적재 / 품질검사)
# =====================================================================
# 설치:
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)
#   pip install -r requirements.txt
#
# ⚠️ Airflow는 의존성 충돌이 잦아 여기 넣지 않는다.
#    Airflow는 Docker로 따로 띄우고 airflow/requirements.txt 를 쓴다.
# =====================================================================

# --- DB 연결 --------------------------------------------------------
psycopg[binary]==3.2.3          # PostgreSQL 드라이버
SQLAlchemy==2.0.36              # 적재 스크립트에서 사용

# --- 데이터 생성 -----------------------------------------------------
Faker==33.1.0                   # 이름·텍스트 등 그럴듯한 더미값
numpy==2.2.0                    # 분포 샘플링 (리텐션 곡선, 편중 등)
pandas==2.2.3                   # CSV 조립·검증

# --- 설정 -----------------------------------------------------------
python-dotenv==1.0.1            # .env 읽기
PyYAML==6.0.2                   # 생성 파라미터 yaml

# --- 외부 API -------------------------------------------------------
requests==2.32.3                # NEIS API 호출
tenacity==9.0.0                 # 재시도 로직

# --- BigQuery -------------------------------------------------------
google-cloud-bigquery==3.27.0
google-cloud-storage==2.19.0
pyarrow==18.1.0                 # parquet 변환 (적재 속도)

# --- 개발 편의 (선택) -------------------------------------------------
rich==13.9.4                    # 진행률·표 출력. 실행 상황을 보기 쉽게
