# =====================================================================
# requirements.txt · 지금 실제로 쓰는 패키지만
# =====================================================================
# 설치:
#   python -m venv .venv
#   .venv\Scripts\activate          (PowerShell / cmd)
#   source .venv/Scripts/activate   (Git Bash)
#   pip install -r requirements.txt
#
# 원칙: 쓰지 않는 패키지는 적지 않는다.
#   아직 안 쓰는 것까지 미리 적어두면 설치가 무거워지고(수백 MB),
#   버전 충돌 위험만 늘어난다. 필요해지는 단계에서 그때 추가한다.
# =====================================================================

psycopg[binary]==3.3.4      # PostgreSQL 드라이버 (적재, RLS 검증)
python-dotenv==1.2.2        # .env 읽기
PyYAML==6.0.1               # 합성 데이터 분포 파라미터


# ---------------------------------------------------------------------
# 앞으로 단계별로 추가될 것 (지금은 설치하지 않는다)
# ---------------------------------------------------------------------
# P3 NEIS 수집:
#   requests, tenacity
#
# P4 BigQuery 적재:
#   google-cloud-bigquery, google-cloud-storage, pyarrow
#
# 데이터 분석이 필요해지면:
#   pandas, numpy
#
# ⚠️ Airflow 는 여기 넣지 않는다. 의존성 충돌이 잦아 Docker 로 따로 띄우고
#    airflow/requirements.txt 를 쓴다.
