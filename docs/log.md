---
title: 작업 이력
group: 위키
tags: [위키, 이력]
---

# 작업 이력

위키에 **무엇을 언제 했나.** 덧붙이기만 하고 고치지 않는다.

git log 와 다르다 — git 은 파일이 어떻게 바뀌었는지를 남기고, 여기는
**원본을 반영했는지 · 색인을 다시 세웠는지 · 검사를 돌렸는지**를 남긴다.
회의록을 넣고 반영을 잊어버리는 것이 제일 흔한 사고라 그걸 잡는 것이 목적이다.

형식: `## [YYYY-MM-DD] 연산 | 한 줄`

연산은 셋이다 — `ingest`(원본을 읽어 위키에 반영) · `query`(물음에 답하고
답을 남김) · `lint`(어긋난 곳을 찾아 고침). 자세한 절차는 [[CLAUDE|CLAUDE.md]].

---

## [2026-08-06] ingest | 종합 보고서 24쪽 — 프로젝트를 모르는 사람에게

`docs/PROJECT-REPORT.pdf`. 지금까지의 리포트는 전부 **한 국면**을 다뤘다 —
EDA 는 합성 데이터, 파이프라인 그림은 저장소 셋, 위키 리포트는 문서 구조.
**처음 온 사람에게 건넬 한 장이 없었다.**

**구성 12장** — 구 서비스 결함(출발점) · 프로젝트 정의 · 연표 5국면 ·
산출물 넷(스키마 · 웹앱 · 파이프라인 · 합성) · 위키화의 명분 · 실패 기록 ·
현재 상태 · 남은 일 · 용어.

**세 가지를 명시적으로 썼다**(작업자 지시).
- **결함에서 시작한다** — 탈퇴에 user_id 없음 · 하트 원장이 잔액을 설명 못 함 ·
  관계 39개 중 FK 17개. 서비스 실태(10개교 · 리텐션 63.9%)와 **분리**해서 적었다.
  이 프로젝트의 출발점은 후자다
- **웹앱은 검증 장치다** — 스키마 검증 · 파이프라인 시험 · 실데이터 확보 셋으로
  갈라 적고, 실제로 무엇을 검증했는지(정합성 3종 교정 · safeupdate)를 이었다
- **위키화의 명분** — 문제 셋(조용히 낡음 · 결정이 커밋 메시지에만 · 단일 파일
  읽기 비용)에 대한 대응으로 서술. 절감은 91% 가 아니라 **34%** 로 적었다

**남은 일 7가지**에 합성 데이터 분석(1순위) · P7 의 **슬랙 일일 리포트**를 넣었다.
분석을 stg/mart 보다 앞에 둔 이유도 적었다 — 어떤 집계가 반복되는지는 해봐야 안다.

**옵시디언 그래프 이미지**를 8장에 실었다(`docs/img/obsidian-graph.png`).
저장소 최초의 이미지 자산이다.

**7장을 전면 확장했다**(24쪽 → 29쪽). 처음에는 12개월 판본만 적어 **1개월 샘플
세 판본이 통째로 빠져 있었다.** `synthetic-data-decision-history.html` 의 원장을
근거로 다시 썼다.

  아홉 판본     초판 786만(폐기) · 1m v1~v3(500명) · 12m v1~v5
  단계별 건수   0단계 결정 30 · 1단계 9 · 2단계 11 · 3단계 17 = **67건**
                (결정 30 · 결함 26 · 전환 7 · 해결 1 · 미해결 3)
  30문답        A~G 7묶음별 문항 수와 **유지 18 / 변경 12**
  뒤집힌 결정   **12건** — 리포트 표를 세어 확인했다(reports.md 의 "8건"은 v5 이전 수치)
  결함 26건     11건이 "설정에 값은 있는데 코드가 안 읽는다" · 15건이 시간축의 형태

가장 많이 뒤집힌 묶음이 **E(질문과 투표)** 라는 것을 표에서 드러냈다 — 숫자로 정할
수 있는 것은 유지됐고 **행동의 형태**를 정한 것은 대부분 구현에서 뒤집혔다.

**문체를 공식 문서로 맞췄다** — 제목은 사실 서술, 수치는 표로, 평서형 통일.
`PROJECT-JOURNEY` 는 **건드리지 않았다.** 7/31 시점의 스냅샷이라 덮어쓰면
"그때 무엇까지 알고 있었나"가 사라진다.

## [2026-08-05] ingest | 합성 데이터 v4 확정 후보 — 결정 11개를 노드로

8/3~8/5 사흘간 합성 데이터를 **네 번** 만들고 세 번 버렸다. 그동안 정한 것이
커밋 메시지와 설정 주석에만 있어서 결정 노드로 옮겼다.

**새 결정 노드 11개**

  성장 곡선     growth-curve-two-channels · spring-spike-growth-curve
  구조          school-sequential-adoption · reactivation-cohort ·
                retention-quarter-tier · class-size-for-class-scope
  행동          daily-rhythm-night-peak · never-voters-by-friend-count
  측정·기록     expired-session-status · sensitive-question-report-weight
  파이프라인    row-cap-to-query-cap · generator-emits-updated-at

`link_decisions.py` 에 짝 18개를 넣어 기존 결정과 이었다. 결정 76개 · 링크 평균 2.2개.

**새 리포트 5개** — `docs/reports.md` 를 만들어 목록 노드로 뒀다.
PDF·HTML 24개가 마크다운이 아니라 그래프에 안 보이던 것을, 옵시디언
`showAttachments` 를 켜고 마크다운 링크로 이어 붙였다.

  synthetic-data-decision-history   30문답 · 판본 6개 · 뒤집힌 결정 8건 · 교훈
  EDA-final-12m-v{2,3,4}             전면 감사 · 폐기 판본 기록 · 확정 후보
  pipeline-overview                  저장소 셋이 어디서 갈라지고 만나는가

**고친 것**

  CLAUDE.md   630 → 559줄. 중복 117줄을 걷었다(합성 v2 이야기 · W0~W19 표).
              ⚠️ "세 가지 연산"과 "되돌리기"는 **CLAUDE.md 밖에 없어** 안 건드렸다.
  README      786만 행 등 v1 시절 수치 3곳
  ops-synthetic-data  3단계 → 스모크·프로브·최종. **적재 전 CSV 검증** 절 신설.
              지표 목표 4개 추가(시간대·계절성·같은 반)
  link_decisions.py   꼬리말이 없는 노드에서 블록이 frontmatter 안으로
              들어가 YAML 이 깨지던 버그

**lint 통과.** 볼트 md 143개 · 고아 노드는 `raw/meetings/_template` 하나뿐.

## [2026-07-31] lint | 문서가 주장하는 숫자를 살아 있는 DB 와 대조

낡은 주장 9곳을 고쳤다. README 의 W 단계 표가 순서가 뒤엉켜 있었고(W8→W16→
W17→W19→W18→W9…), P1·P2 가 "완료"인데 합성 데이터는 이미 폐기된 상태였다.
CLAUDE.md 의 "비어 있는 테이블" 목록은 내용이 틀려 있었다 — `report`(3행)와
게시판 4표를 비었다고 적어두고, 정작 빈 `question_request`·`user_withdrawal`
은 빠져 있었다.

**손으로 관리하는 목록은 낡는다**는 것이 이날의 교훈이고, 그 목록을
`db/erd.py` 가 세도록 옮겼다.

## [2026-07-31] ingest | DECISIONS.md 를 노드 56개로 분해

한 파일 24,946 토큰이었다. 결정 하나를 보려고 전체를 읽어야 해서 낭비가
98.2% 였다. `docs/decisions/` 로 쪼개고 `DECISIONS.md` 를 색인으로 남겼다
(기존 `[[DECISIONS]]` 링크 19곳이 그대로 산다).

본문에 손으로 적어두던 대체 관계를 frontmatter 로 올렸다 —
`status: superseded` + `superseded_by`. 지금 2건.

## [2026-07-31] ingest | CLAUDE.md 에서 참조성 절 5개를 docs/ops/ 로

매 세션 자동으로 읽히는 파일이라 10,031 → 7,873 토큰으로 줄였다.
**안전 규칙은 하나도 빼지 않았다** — 조건부로 읽히는 규칙은 안 읽힐 수 있고,
그 순간 규칙이 없는 것과 같다. 대신 뺀 절마다 함정 이름이 든 한 줄을 남겼다.

## [2026-07-31] ingest | 구 서비스 분석 리포트를 raw/ 로 들임

리포트 9종 + PDF 2종. ping-v2 의 거의 모든 문서가 이 숫자를 인용하는데
(탈퇴 70,764건, 잔액 20억, 가입자 67.7만) **원본이 저장소 밖에 있었고 git 으로
추적되지도 않았다.** 인용만 있고 출처가 없는 상태였다.

## [2026-07-31] ingest | 3계층 위키 구조를 세움

`raw/`(원본) · `docs/`(위키) · `CLAUDE.md`(규약). 색인과 이 이력 파일을 만들고
`db/wiki_index.py` 로 색인을 자동 생성하게 했다.

## [2026-07-31] ingest | 테이블 엔티티 페이지 40장을 뽑음

표 하나를 이해하려면 네 군데를 따로 열어야 했다 — DDL 주석 · 결정 노드 ·
정합성 검사 · RLS 정책. `heart_transaction` 은 결정 6개와 검사 5종에 걸쳐 있다.

`db/wiki_tables.py` 가 그 넷을 모아 `docs/tables/<표>.md` 한 장으로 만든다.
새로 쓰는 내용은 없고 전부 기존 파일에서 긁어온다.

곧 합성 데이터를 만들 때 표 40개를 하나씩 놓고 "이 컬럼에 무슨 값이 들어가야
하고 왜 그런가"를 봐야 하는데, 그때 네 군데 찾는 일이 40번 반복되는 것을 막는다.
페이지마다 **생성기가 그 표를 만드는지**를 표시했다 — 지금 14개가 미구현이다.

## [2026-07-31] ingest | 위키 개편 보고서 (PDF)

문서를 LLM 위키로 바꾼 경위·이득·구조를 8쪽으로 정리했다.
`docs/WIKI-REPORT.html` → Edge 헤드리스로 인쇄.

남은 비용도 함께 적었다 — `docs/index.md` 자체가 5,889 토큰이라 전체 탐색
때는 그 값을 낸다는 것, 파일이 늘면 읽기 호출 오버헤드가 붙는다는 것,
세션 전체로 보면 토큰 대부분은 여전히 도구 출력이라는 것.

## [2026-07-31] lint | 위키 보고서의 절감 수치를 바로잡음

"91% 절감"이라 적었던 것을 고쳤다. 통째로 읽기(25,066)를 기준으로 삼았는데
그건 **게으른 선택이었지 유일한 방법이 아니었다** — 한 파일이어도 grep 으로
구간만 읽으면 1,928 이었다.

질문 5개로 다섯 경로를 실측한 결과:

  A 통째로 읽기           25,066   쪼개기 전 실제로 하던 것
  B grep + 구간 읽기       1,928   쪼개기 전에도 가능했던 것
  C 색인 + 노드 하나       2,656   보고서에 쓴 경로 (B보다 무겁다)
  D grep + 정확한 노드     1,263   쪼갠 뒤 실제로 하게 되는 것
  E grep + 매칭 전부       6,907   B보다 2.6배 나쁘다

공정한 비교는 B 대 D 이고 절감은 **34%** 다. 그리고 E 가 실재하는 위험이라
CLAUDE.md 의 query 절차에 "골라 읽는다"를 규칙으로 박았다.

토큰보다 중요한 이득은 따로 있다 — **경계가 의미와 일치한다.** B 경로는
결정의 시작과 끝을 모르므로 읽을 범위를 찍어야 하고, 잘려도 알 수 없다.

## [2026-07-31] lint | 그래프에서 결정 56개가 통째로 사라졌던 것을 고침

`-path:"DECISIONS"` 로 루트 색인 하나만 빼려 했는데, **옵시디언 `path:` 는
대소문자를 구분하지 않아** `docs/decisions/` 56개가 전부 걸러졌다.
`-path:"DECISIONS.md"` 로 파일명까지 적어 고쳤다.

같이 두 가지를 더 손봤다.

**FK 를 위키 링크로.** 표 페이지의 `→ app_user` 를 `→ [[app_user]]` 로 바꿨다.
옵시디언 그래프에 **스키마 구조가 그대로 그려진다.** 링크 158 → 258.
이제 `app_user` 가 71 연결로 중심인데, 이건 색인 같은 가짜 허브가 아니라
실제로 모든 활동이 유저에 매달려 있다는 사실이다.

**꼬리말의 `[[erd]]` 를 뺐다.** 표 40장이 전부 걸어서 erd 가 링크 41개짜리
가짜 허브가 돼 있었다 — 색인과 같은 문제였다.

**이름 충돌도 정리.** `README` 가 4개(루트·raw·raw/legacy-analysis·web),
`CLAUDE` 가 2개라 `[[README]]` 가 어디를 가리키는지 옵시디언도 모른다.
경로를 붙인 링크로 바꿨다.

⚠️ **결정끼리의 링크가 2개뿐이다** (56개 중 52개가 고립).
한 파일이던 시절에는 나란히 있는 것이 연결처럼 보였지만 실제 관계는 아니었다.
그래프의 밀도를 진짜로 올리려면 여기를 채워야 한다.

## [2026-07-31] lint | 위키 사용 안내에 빠진 것을 채움

세 군데를 점검했더니 팀원이 실제로 필요한 것들이 빠져 있었다.

**CLAUDE.md** — ingest 절차에 `wiki_tables.py` 재생성이 빠져 있었다(색인만
있었다). query 절차는 `docs/decisions/` 만 말하고 표·운영 문서를 안 가리켰다.
그리고 **결정 노드의 frontmatter 형식과 슬러그 규칙이 어디에도 없었다** —
"형식은 기존 파일과 같다"고만 되어 있어 새로 쓰는 사람이 기존 파일을 열어봐야
했다. 생성물을 손으로 고치지 말라는 규칙도 없었다.

**PDF 보고서** — 회의록 → ingest 흐름만 있고 **회의 없이 결정이 날 때**가
없었다. 실제로는 그쪽이 더 흔하다. "처음 열었다면 읽는 순서"와
"하지 말아야 할 것" 표도 넣었다.

**ONBOARDING** — 위키 언급이 **0건**이었다. 팀원이 가장 먼저 여는 문서인데
`docs/decisions/`·`docs/tables/` 가 무엇인지 알 길이 없었다. 폴더 안내에
네 줄을 넣고 "무엇을 찾을 때 어디로 가나" 표를 붙였다.

## [2026-07-31] ingest | 결정끼리 "이어지는 결정" 링크 58쌍

결정 → 결정 링크가 **2개**뿐이었다. 56개 중 52개가 다른 결정과 전혀 안
이어져 있었다 — 한 파일이던 시절엔 나란히 있는 것이 연결처럼 보였지만
실제 관계는 아니었다.

56개를 훑어 **짝마다 왜 이어지는지를 한 줄로 적었다.** 그 한 줄을 못 쓰겠으면
관련된 결정이 아니다 — 같은 표를 건드린다고 관련된 것이 아니고, 기계로 이으면
노이즈가 생기고, 노이즈가 있는 링크는 아무도 안 따라간다.

  결정 → 결정 링크  2 → 118개
  이어진 노드       4 → 56개 (전부)
  노드당 평균       2.1개

`db/link_decisions.py` 에 짝을 데이터로 두었다. 새 결정을 넣은 뒤 PAIRS 에
한 줄 추가하고 다시 돌리면 된다.

**부작용 하나를 함께 고쳤다.** 링크 제목에 표 이름이 들어가면(예:
`drop-admin-user` 의 "app_user.is_admin") 그 표를 다루지도 않는 결정이
`docs/tables/` 에 딸려온다. 표를 찾을 때 "이어지는 결정" 블록은 빼고 보게 했다.

## [2026-07-31] ingest | 위키를 되돌리는 길을 마련

노드 분할이 **오히려 손해로 드러날 경우**에 대비한다. 판단 기준은 둘 —
같은 질문에 토큰을 더 쓰거나, 관련 결정을 놓쳐 답이 나빠지거나.

기준값: 골라 읽기 1,263 · 한 파일 시절 grep 1,928 · 매칭 노드 전부 6,907.
마지막 습관이 굳으면 되돌리는 편이 낫다.

**`git revert` 로는 안 된다** — 되돌리는 커밋 이후에 쓴 결정이 함께 사라진다.
위키로 바꾼 뒤 적은 결정은 노드 파일로만 존재하기 때문이다.

그래서 `db/wiki_merge.py` 를 만들었다. **지금 있는 노드 전부**를 읽어
`DECISIONS.md` 한 파일로 합치므로 새로 쓴 결정도 살아남는다.
`status: superseded` 는 취소선과 화살표로 옮겨진다.
옛 원본과 대조해 **본문 손실 0** 을 확인했다.

`git tag pre-wiki` 가 위키 직전 상태(`c650ca3`)를 가리킨다.
어디까지 되돌리고 무엇을 남기는지는 [[CLAUDE|CLAUDE.md]] 의
"위키를 되돌려야 한다면" 에 적었다 — `raw/` 와 `doc_lint.py` 는 남긴다.

## [2026-08-05] query | v4 확정을 위해 정밀 EDA 를 돌리고 한계 5건을 문서화

"12m-v4 를 최종 확정해야 하니 EDA 를 더 돌려 보고서에 요소를 더하라"는 물음.
로컬 `pgtest` 의 v4 실데이터에 **9개 축을 새로 캐고** 리포트를 다시 뽑았다.

**새로 캔 축** — 인구·학교 계층 / MAU·DAU·stickiness·수명 / 친구 요청 퍼널 /
질문·슬롯·성별 / 하트 원장·결제·ARPU·고래 / 힌트 단계 / 게시판·신고·제재 /
**균등분포 전수 점검(변동계수)** / **정합성이 못 잡는 것**.

**찾은 것 5건** — 전부 오류가 아니라 **경계**다.

| | |
|---|---|
| 접속·투표 세션이 독립 | 겹침 3.84%. 퍼널 불가. 비율이 1.01→6.45 로 드리프트 |
| 시간 간격이 전부 균등 | 균등도 1.0 근처가 5종. 평균만 유효 |
| 탈퇴·BAN 이 활동을 안 멈춤 | 탈퇴 후 하트 거래 128만 건 (탈퇴자의 88.7%) |
| 7개 축에 차이 없음 | 변동계수 0.0001~0.045 |
| `REVEALED` 0건 | 이름 공개 12,103건이 `PARTIAL` 로 남음 |

**부수 발견** — `sessions.weekend_multiplier` · `sessions.hour_weights` 를
생성기가 읽지 않는다. "설정에 값이 있어도 코드가 안 읽으면 없는 것" 함정의
**8·9번째** 사례. 그래서 요일 효과가 없다.

**바로잡은 것** — v4 문서의 "3국면 MAU 진폭 2.2배(목표 미달)"는 산출 기준이
없었고 세 기준 중 어느 것으로도 재현되지 않았다. 기준을 명시해 다시 재니
**4.0~4.5배**로 목표를 넘는다. 미이행에서 **해소**로 옮겼다.

**결정** — 재생성하지 않고 [[confirm-v4-with-known-limits|한계를 문서화하고 확정]].
리포트 13장을 **할 수 있다 17 / 주의 7 / 할 수 없다 14** 로 갈랐다.
`EDA-final-12m-v4.pdf` 8쪽 → **23쪽**.

`db/doc_lint.py` 통과(살펴볼 것 1건은 design-spec 의 기존 이력 서술).

## [2026-08-05] query | 투표 활동 분포를 자세히 파다가 결함 1건을 찾음

"상위 10%가 90%를 던진다는데 정확한 분포를 보고 싶다"는 물음.
십분위 · 분위수 · 히스토그램 · 로렌츠 · 상위 20명으로 갈라 봤다.

**분포 자체** — 중앙값 29표 · 평균 1,224표(42배 차이) · 최대 545,707표.
상위 0.1%(17명)가 15.85%, 상위 1%(167명)가 48.54%. 지니 0.9364.
히스토그램이 **이중 봉우리**다 — 사람은 10~49표 구간(6,789명)에 몰려 있는데
투표는 10,000표 이상 구간(402명)이 66.52%를 가져간다.

**집중의 원인은 친구가 아니라 기간이었다.** 십분위별로 갈라 보니
친구 수는 1분위 44.8 · 8분위 39.8 로 **1.1배**인데 투표 수는 **1,557배**다.
같이 움직이는 것은 활동 기간(91.9일 ↔ 1.3일)과 세션 수(1,053 ↔ 1).

**⚠️ 그러다 결함을 찾았다** — 하루 투표 세션 수에 **상한이 없다.**
하루 최다 2,243세션, 투표에 쓴 시간 최대 **406.8시간/일**,
24시간 초과가 1,251 유저-일(67명). 1위 유저는 하루 554세션 · 99.2시간.
2위는 314일 동안 458,887번 던지고 **한 번도 지목받지 못했다.**
16위는 **친구 8명으로 98,862번** 던졌다.

정합성 17종·이상치 9종이 이걸 안 묻는다 — "하루에 몇 번까지가 사람인가"는
스키마 제약으로 표현되지 않는다.

**확정 결정은 유지하되 성격을 갈랐다** — 앞의 5건은 "이 축으로는 답을 못 한다"(경계),
이건 "답이 틀리게 나온다"(결함). 영향이 꼬리 0.5%에 갇혀 있어 두 규칙
(상위 1% 안 봄 · 집중 지표 절대값 인용 안 함)으로 막고, **v5 1순위**로 올렸다.

리포트에 **7-2장**이 통째로 붙었다. 23쪽 → **27쪽**.

## [2026-08-05] ingest | 합성 데이터 v5 — 요구사항 7가지를 구현하고 전면 EDA

작업자가 지정한 7가지를 구현했다. v4 확정 직후 정밀 EDA 에서 나온
**결함 1건 · 경계 5건**을 닫는 작업이기도 하다.

| # | 요구 | v4 → v5 |
|---|---|---|
| 1 | 시간 간격 비균등 | 균등도 1.01~1.05 → **0.15~0.54** |
| 2 | 요일 패턴 (구 서비스) | CV 0.024 → **0.18~0.24**, 순위 7개 일치 |
| 3 | 행동은 세션 안에서만 | 3.84% → **99.99%** |
| 4 | 세션 로그 노이즈 | 없음 → 중복 행 **0.399%** |
| 5 | 탈퇴 후 로그 금지 | 9종 발생 → **9종 전부 0** |
| 6 | 투표 시간 일관성 | 19.50% 어긋남 → **0.00%**, 상관 0.0007 → 0.7005 |
| 7 | 좋아요 유저 60% | 95.9% → **59.6%** |

**⚠️ 요일 패턴이 지시와 달랐다.** 작업자는 "월→일 우하향"이라 했는데
원본(`raw/legacy-analysis/08_attendance_feature.md`)은 **V자**였다 —
금요일이 최저이고 주말에 회복한다. 원본을 따르기로 정하고 그 근거를 남겼다
([[dow-from-legacy-attendance]]).

**⚠️ 하루 상한을 개수로 걸었다가 되돌렸다.** 12세션으로 자르니 헤비유저가
통째로 사라져 행이 반토막(9,509만) 나고 지목 쏠림이 39% 로 주저앉았다.
작업자가 "상한을 정하라는 지시는 없었고 물리적 불가능만 없애라"고 바로잡아,
**시간 예산**(투표 6h/일)으로 바꿨다. 1억 2,370만 행 · 쏠림 49.8% 로 회복.

**`zipf_alpha` 0.45 → 1.05.** 쏠림의 바닥이 내려가서다 — v4 의 48.8% 는
인기도가 아니라 **불가능한 활동 꼬리**가 떠받치고 있었다. 이제 매력도로 만든다.
프로브 곡선을 설정 주석에 새로 적었다(옛 곡선도 함께 남김).

**결과** — 정합성 17종 0 · 이상치 9종 0 · **v4 의 "할 수 없다" 15종 중 9종 해금.**
리포트 `EDA-final-12m-v5.pdf` **23쪽**.

결정 노드 4개: [[lognormal-not-uniform]] · [[dow-from-legacy-attendance]] ·
[[session-bounded-actions]] · [[withdrawal-is-terminal]].
[[confirm-v4-with-known-limits]] 는 `superseded` 로 돌렸다.

**남긴 것** — 광고 시청시간 로그정규 **누락**(7곳 중 1곳) · BAN 이후 활동 2,693건
(요구는 탈퇴였고 제재는 범위 밖) · 시간대 대비 5.3→3.1배(세션 병합의 부작용).
그리고 **`sessions.votes_per_app_session` 이라는 죽은 설정을 또 만들었다** —
"설정에 값이 있어도 코드가 안 읽으면 없는 것" 함정을 같은 작업 안에서 다시 밟았다.

## [2026-08-06] ingest | 합성 v5 를 BigQuery 에 올렸다 — 파티션 한도에 한 번 막히고

**1억 2,369만 행 · 40개 표**가 `raw` 에 들어갔다. `verify_load.py` 로 Postgres 와
대조해 **40개 표 전부 일치**. 실유저(`_source='supabase'`)는 그대로 살아 있다.

**CSV 를 다시 만들 필요가 없었다.** `pgtest` 컨테이너가 정지 상태로 살아 있었고
v5 가 그대로 들어 있었다(CSV 와 행 수가 정확히 일치). 처음에 "로컬 DB 가 없다"고
본 것은 **Docker 데몬이 켜지는 중이라 컨테이너 목록을 못 읽은 것**이었다 —
데몬이 꺼진 것과 컨테이너가 없는 것은 다르다. CLAUDE.md 의 "지금 없다"도 고쳤다.

**⚠️ 파티션 수정 한도에 막혔다.** `vote_candidate` 2,620만 행에서 403 —
`Number of partition modifications to a column partitioned table`.
파티션 테이블은 **하루 4,000회**만 받고, 적재 작업 하나가 N개 파티션에 걸치면
N회를 먹는다. 저장 순서대로 꺼내니 20만 행 덩어리가 30일치에 흩어져
작업당 30회씩 나갔고 131건에서 찼다.

**`ORDER BY` 파티션 컬럼 한 줄로 풀었다** — 한 덩어리가 하루치에 들어가
작업당 1~2회로 떨어진다. `updated_at` 에 인덱스가 있어 Index Scan 이라 정렬
비용은 0이다. 재적재 6,708만 행이 오류 없이 끝났고 **속도가 1.8배** 빨라졌다
(초당 12,732 → 23,215행).

**틀렸던 것 둘.** ①앞서 `--batch-rows` 를 열며 "적재 작업 1,500건 한도에
여유가 생긴다"고 적었는데 **실제로 막은 것은 다른 한도**였고 배치 크기는
무관했다([[bulk-load-batch-rows]] 에 정정을 달았다). ②적재 중 속도가 느려지는
것을 "정상 동작"이라 판단했는데 **한도가 차오르는 신호**였다. ③한도 초기화를
태평양 자정(KST 17시)으로 추정해 "오후까지 기다려야 한다"고 했으나, 실제로
시도하니 이미 풀려 있었다 — **추정하지 말고 시도해서 확인할 일이었다.**

**저장은 8.74 GiB.** CSV 11.0GB 가 열 압축으로 줄었다. 무료 한도 10 GiB 안이라
과금 없음. 다만 **여유가 1.26 GiB 뿐**이라 ops 문서에 적어 뒀다.

결정 노드 1개: [[partition-ordered-extract]]. `--exclude` 도 함께 열었다.

## [2026-08-06] ingest | 팀원이 BigQuery 를 볼 수 있게 — 키는 주지 않는다

**권한은 이미 열려 있었다.** IAM 에 팀원 4명(`ahn960712`·`dpddpd0612`·
`fr1edrices1213`·`lawjin0330`)이 **BigQuery 관리자**로 붙어 있다. 각자 자기
Gmail 로 콘솔에 들어가면 바로 쿼리가 된다 — 추가 조치가 필요 없었다.

**"코드로 보려면 권한을 더 줘야 하고 키를 전달해야 한다"는 말이 돌았는데 틀렸다.**
권한(무엇을 할 수 있는가)과 인증(내가 나임을 증명하는 방법)을 섞은 이야기다.
BigQuery 관리자는 이미 이 서비스의 최대 역할이고, 코드 접근에 모자란 것은
`gcloud auth application-default login` 한 번뿐이다. **키 파일은 안 만든다.**

`credentials.json` 을 나누면 ①모든 쿼리가 `airflow-loader` 로 기록돼 누가 뭘
했는지 사라지고 ②유출 시 무효화하면 **적재 파이프라인이 같이 죽는다.**

**[[ops-bigquery-team-access]] 를 만들었다** — 팀원에게 그대로 건네는 문서다.
콘솔·파이썬·BI 세 경로, 쿼리 규칙 넷, 비용, 주지 말아야 할 것.

**조인 함정을 실측했다.** `vote_item` × `app_user` 를 `_source` 없이 조인하면
**14,286행이 부풀려진다**(13,459,564 → 13,445,278). 실유저 행은 사실상 전부
합성과 id 가 충돌한다 — `app_user` 23명 전원, `vote_item` 726건 전원.
`stg` 층이 없어 팀원이 `raw` 를 직접 보므로 이 함정이 지금 살아 있다.
문서의 SQL 예시 셋은 전부 실제로 돌려서 확인했다.

**권한은 좁히지 않기로 했다**(작업자 판단). 대신 팀원이 표를 지울 수 있다는 것과
`tmp_` 접두어 규칙을 문서에 명시했다.

## [2026-08-06] ingest | 팀원용 BigQuery 접속 시험 노트북

`notebooks/00-bigquery-connection-test.ipynb` — 팀원이 열어 위에서 아래로
실행하면 접속이 됐는지 **스스로 진단하는** 노트북이다.

**핵심은 1번 셀이다** — `google.auth.default()` 로 자격증명 종류를 보고
**서비스 계정이면 경고를 띄운다.** 누군가에게 키 파일을 받아 쓰고 있다는 뜻이라,
"키는 주고받지 않는다"는 규칙이 문서의 문장이 아니라 **코드로 검사된다.**

나머지는 dry run 으로 비용 미리 재기 · 첫 쿼리 · 규칙 ①(`_source` 조인) ·
규칙 ③(UTC→KST) 실증. 마지막에 증상별 해결 표 7행.

**셀의 SQL 을 전부 실제로 돌려서 확인했다.**
- 조인 함정: `vote_session` × `app_user` 에서 **1,759행이 부풀려진다**
- 시간대: **UTC 봉우리 13시 · KST 봉우리 22시** — 문서의 "9시간 밀림"이 그대로 재현
- 전체 실행 비용 **0.19 GiB** (월 한도의 0.02%)

노트북은 생성 스크립트(`notebooks/_make_connection_test.py`)로 만든다.
손으로 고치지 말고 스크립트를 고쳐 다시 뽑는다 — 셀 id 정규화까지 거기서 한다.

## [2026-08-06] ingest | 합성 데이터 재현성을 실측하고, 덤프 대신 재생성으로 정했다

팀원이 로컬에서도 합성 데이터를 쓰려면 **11GB 덤프를 나눠줘야 하나**가 질문이었다.
답은 **아니오** — 다시 만들면 같은 것이 나오기 때문이다.

**설정 주석의 주장을 처음으로 검증했다.** `synthetic-v2.yaml` 에 "같은 시드 +
같은 config = 같은 데이터"라고 적혀 있었지만 확인된 적이 없었다.
전체 규모(2만 명·12개월)를 다시 만들어 기존 생성물과 sha256 으로 대조 —
**CSV 33개 전부 일치.** 팀원이 만든 데이터가 BigQuery 의 것과 같음이 보장된다.

**생성 시간도 실측했다 — 14분 19초.** ONBOARDING 의 "1~2시간"은 틀렸고,
같은 문장 안에서 "생성 12분 + 적재"라고 자기모순까지 있었다.

**ONBOARDING 에서 고친 것 셋**
- `SYNTHETIC_SEED` — **코드 어디에도 없는 이름**이었다. 실제는 `meta.seed`
- `적재 완료 — 총 7,8xx,xxx 행` — v1(786만) 시절 숫자. 지금은 1억 2,369만
- 전체 규모에 `--users 20000 --months 12 --schools 50` 을 주라고 했으나
  설정 파일에 이미 있어 **옵션이 필요 없다**

**PG 적재는 선택이라는 것을 분명히 했다.** `generate.py` 는 DB 를 안 쓴다
(psycopg 참조 0건). BigQuery 나 pandas 로 볼 거면 PG 가 아예 필요 없고,
로컬 SQL 연습·정합성 검사에만 필요하다. 넣을 때의 선행 조건 셋(컨테이너 ·
`db/apply.py --target local` · `load.py`)과 **`.env` 가 필요 없다는 것**
(코드에 기본값이 있다)을 [[ops-bigquery-team-access]] 에 적었다.

⚠️ 함께 적은 경고 — **CSV 를 손으로 `\copy` 하면 안 된다.** `load.py` 가
이어서 도는 95(시퀀스)·96(워터마크)을 건너뛰게 되고 **둘 다 조용히 틀린다.**

## [2026-08-13] lint | 죽은 힌트 요금을 인용했다 — DDL 주석 다섯 군데를 고치고 지표를 정했다

마트에 들어가기 전 **핵심 지표를 뽑는 대화**에서 시작했는데, 그 과정에서
힌트 요금을 **200→300→500→1000 누진**이라고 적었다. **반년 전에 없어진
요금제**다. 현행은 기본 5종 각 20하트 + 이름 공개 100하트(W14).

**근거는 `db/ddl/30_question_vote.sql` 의 주석이었다.** 그 문장은 쓰인 시점엔
맞았고, 마이그레이션 006 이 구조를 갈아치우면서 주석만 남았다.

**`db/apply.py` 는 DDL + 마이그레이션을 전부 돌린다.** 그러니 `db/ddl/` 은
현재 스키마가 아니라 **출발 상태**다. 진실은 파일 하나가 아니라 **순서**에 있다.
제일 고약한 것 — 006 은 자기가 바꾼 사실을 `COMMENT ON COLUMN` 으로 **DB 안에는
정정해 넣었다.** 정정이 도달하지 않은 곳은 **사람이 읽는 파일**뿐이었다.

**마이그레이션 12개를 DDL 과 전수 대조했다.** `10`·`20`·`50` 은 잘 반영돼 있었고
(`20_social.sql` 은 "(011)" 을 인라인으로 달아 뒀다), 두 파일이 낡아 있었다.

| 고친 곳 | 낡아 있던 것 |
|---|---|
| `30` `hint_purchase` | 누진 요금 · `step` 의 뜻(단계→**순서**) · `heart_cost > 0`(→`>= 0`) · `uq_hint_step`(→`uq_hint_kind`) |
| `30` `vote_received` | 008 답장 · 004 `updated_at` 이 안 보이고, 007 이 옮긴 `hint_char_index` 를 여기서 찾게 돼 있었다 |
| `30` `vote_item` | 003 `padded_count` 가 안 보였다 |
| `60` **전체** | "**익명** 게시판 · v1 에서 **개통하지 않는다**" — W9 가 닉네임 게시판으로 **이미 열었다** |
| `10` `school` | "`neis_school_code` 는 API 연동 키" — 001 의 `neis_office_code` 와 **한 쌍이어야 부를 수 있다** |

**주석만 고쳤다. SQL 문장은 한 줄도 안 건드렸다** — 그 문장들은 재생성의
출발점이고, 마이그레이션이 그 위에서 도는 것을 전제한다. 현행으로 "고치면"
006 이 이미 있는 제약을 다시 만들다 깨진다.

**노트북 4개에서 분석 규칙을 뽑아 [[ops-analysis-conventions]] 로 적었다.**
새로 정한 게 아니라 **이미 지켜지고 있는 것**을 드러냈다 — `_source='local'` ·
`_deleted_at IS NULL` · 탈퇴자 제외는 네 곳 다 완비다. 대신 셋이 비어 있었다.

- **activation 에 `Asia/Seoul` 이 0회.** 지금은 일수 차이만 봐서 안 드러나는데,
  **날짜로 자르는 순간** 9시간 밀린다. 지금 틀린 게 아니라 다음 한 줄에서 틀린다
- **제재 유저를 아무 데서도 안 거른다.** 넷 다 탈퇴만 걸렀다. 탈퇴는 종점이지만
  **제재는 아니다** — v5 에 제재 뒤에도 투표하는 유저가 34명 있다.
  유저 단위가 아니라 **`starts_at` 이후 행**으로 잘라야 한다
- **`revenue` 의 `hint_purchase_count` 가 광고 무료 힌트를 구매로 센다.**
  같은 셀의 `SUM(heart_cost)` 는 0 이 더해질 뿐이라 멀쩡하다 —
  **소비량은 맞고 횟수만 부풀어 있다**

**지표는 10개로 고정했다**([[core-metrics-v1]]). North Star 는 주간 투표 완주자.
일간 모니터링에서는 **코호트 리텐션을 빼고 DAU 를 상태로 쪼갠다**(신규/잔존/복귀/이탈)
— 코호트는 시차가 구조적이라 매일 보는 자리에 맞지 않는다. 실유저(20~50명)는
비율 자체가 노이즈라 절대수로 본다.
