---
title: 작업 이력
group: 위키
tags: [위키, 이력]
---

# 작업 이력

위키에 **무엇을 언제 했나.** 덧붙이기만 하고 고치지 않는다.

git log 와 다르다 — git 은 파일이 어떻게 바뀌었는지를 남기고, 여기는
**원본을 반영했는지 · 색인을 다시 세웠는지 · 검사를 돌렸는지**를 남긴다.
회의록을 넣고 반영을 잊어버리는 것이 제일 흔한 사고라 그걸 잡는 것이 목적이다.

형식: `## [YYYY-MM-DD] 연산 | 한 줄`

연산은 셋이다 — `ingest`(원본을 읽어 위키에 반영) · `query`(물음에 답하고
답을 남김) · `lint`(어긋난 곳을 찾아 고침). 자세한 절차는 [[CLAUDE|CLAUDE.md]].

---

## [2026-07-31] lint | 문서가 주장하는 숫자를 살아 있는 DB 와 대조

낡은 주장 9곳을 고쳤다. README 의 W 단계 표가 순서가 뒤엉켜 있었고(W8→W16→
W17→W19→W18→W9…), P1·P2 가 "완료"인데 합성 데이터는 이미 폐기된 상태였다.
CLAUDE.md 의 "비어 있는 테이블" 목록은 내용이 틀려 있었다 — `report`(3행)와
게시판 4표를 비었다고 적어두고, 정작 빈 `question_request`·`user_withdrawal`
은 빠져 있었다.

**손으로 관리하는 목록은 낡는다**는 것이 이날의 교훈이고, 그 목록을
`db/erd.py` 가 세도록 옮겼다.

## [2026-07-31] ingest | DECISIONS.md 를 노드 56개로 분해

한 파일 24,946 토큰이었다. 결정 하나를 보려고 전체를 읽어야 해서 낭비가
98.2% 였다. `docs/decisions/` 로 쪼개고 `DECISIONS.md` 를 색인으로 남겼다
(기존 `[[DECISIONS]]` 링크 19곳이 그대로 산다).

본문에 손으로 적어두던 대체 관계를 frontmatter 로 올렸다 —
`status: superseded` + `superseded_by`. 지금 2건.

## [2026-07-31] ingest | CLAUDE.md 에서 참조성 절 5개를 docs/ops/ 로

매 세션 자동으로 읽히는 파일이라 10,031 → 7,873 토큰으로 줄였다.
**안전 규칙은 하나도 빼지 않았다** — 조건부로 읽히는 규칙은 안 읽힐 수 있고,
그 순간 규칙이 없는 것과 같다. 대신 뺀 절마다 함정 이름이 든 한 줄을 남겼다.

## [2026-07-31] ingest | 구 서비스 분석 리포트를 raw/ 로 들임

리포트 9종 + PDF 2종. ping-v2 의 거의 모든 문서가 이 숫자를 인용하는데
(탈퇴 70,764건, 잔액 20억, 가입자 67.7만) **원본이 저장소 밖에 있었고 git 으로
추적되지도 않았다.** 인용만 있고 출처가 없는 상태였다.

## [2026-07-31] ingest | 3계층 위키 구조를 세움

`raw/`(원본) · `docs/`(위키) · `CLAUDE.md`(규약). 색인과 이 이력 파일을 만들고
`db/wiki_index.py` 로 색인을 자동 생성하게 했다.
